# ステータス遷移ドキュメント (keiba-app)

このドキュメントは、keiba-app の各エンティティとUI画面のステータス遷移を定義しています。
リファクタリング時に、**仕様変更がないこと** を保証するために使用します。

**スコープ**: コア業務フロー（認証 → レース管理 → 馬券購入 → 払戻管理）に限定

---

## 1. ユーザー認証フロー

未認証 → ログイン画面 → パスワード入力 → セッション取得 → 認証済み

- `GET /api/auth/check` で初期化時にセッション確認
- `POST /api/auth/login` でパスワード認証
- `authedFetch()` で 401 時に自動的にログイン画面に戻す

---

## 2. レース（race）エンティティのライフサイクル

| 状態 | entries | finish_order | payouts | 可能な操作 |
|------|---------|--------------|---------|-----------|
| 作成直後 | [] | NULL | NULL | entries更新 |
| 出走馬登録済み | [...] | NULL | NULL | finish_order更新、削除 |
| 結果確定 | [...] | [...] | NULL | payouts更新、削除 |
| 払戻確定 | [...] | [...] | {...} | 削除 |

---

## 3. 馬券（ticket）エンティティのライフサイクル

| 状態 | payout | 表示 | 計算に含める | 可能な操作 |
|------|--------|------|------------|-----------|
| 購入直後 | NULL | グレー | 購入金額のみ | 削除 |
| 払戻確定 | 0 or 数字 | 確定色 | 購入金額+払戻金額 | 削除 |

---

## 4. UI画面遷移

```
ログイン画面
    ↓
購入履歴 (index.html)
    ├─ 馬券購入 (buy.html)
    ├─ 集計 (stats.html)
    └─ レース管理 (races.html)
        ↓（各画面から任意の画面に遷移可能）
```

---

## 5. API エンドポイントのステータス遷移への影響

| エンドポイント | メソッド | 効果 | ステータス変化 |
|-------------|---------|------|-------------|
| `/api/races` | POST | レース作成 | - → 出走馬未登録 |
| `/api/races/:id` | PUT | レース編集 | 各状態間の遷移 |
| `/api/races/:id` | DELETE | レース削除 | 全て削除 |
| `/api/tickets` | POST/bulk | 馬券登録 | - → 払戻未確定 |
| `/api/tickets/:id` | PUT | 払戻更新 | 払戻未確定 → 払戻確定 |
| `/api/tickets/:id` | DELETE | 馬券削除 | 全て削除 |

---

## 6. リファクタリング時の検証チェックリスト

```
[ ] 認証フロー
  [ ] ログイン/ログアウトが正常に動作
  [ ] セッション切れ時に自動的にログイン画面に戻る
  [ ] 401 エラーハンドリングが正しい
  
[ ] レース管理
  [ ] レース作成直後: entries=[], finish_order=null, payouts=null
  [ ] 出走馬登録後: entries=[{...}], finish_order=null, payouts=null
  [ ] 着順入力後: finish_order=[...]
  [ ] 払戻入力後: payouts={...}
  [ ] レース削除時に関連する tickets も削除される
  
[ ] 馬券管理
  [ ] 購入直後: payout=null
  [ ] 払戻入力後: payout=0 or payout=数字
  [ ] 払戻未確定時: stats.html で「実質マイナス」表示
  [ ] 払戻確定時: stats.html で正確な収支・回収率を表示
  
[ ] 計算ロジック
  [ ] combos.js: 組み合わせ生成が正確
  [ ] payout.js: 的中判定が正確
  [ ] stats.js: 回収率・的中率計算が正確