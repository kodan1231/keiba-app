> `docs/DESIGN.md` から機能単位で分割したファイル。現状の仕様のみ記載。
> 「〜」参照 は `docs/design/` 内の見出し名(`grep -rn "見出し" docs/design/` で辿れる)。
> 過去の経緯・完了履歴は `archive/documents/BACKLOG_HISTORY.md`。

# CSV取込の仕様

- CSV 1行を購入グループとして保持し、組み合わせを個別買い目へ分解する
- 的中買い目は `is_hit=1`、払戻を該当買い目へ紐付ける
- **着順・払戻レートの反映**: CSV取込結果のうち、着順が一意に定まる情報は`races.finish_order`
  へ、的中組み合わせごとの払戻レート(100円あたり)は式別ごとに`races.payouts`へ反映する
  - 着順: 単勝(1着のみ)・馬単(1・2着)・三連単(1〜3着)の的中が1行のみの場合に反映する。
    三連単からの情報を最も信頼できるものとして扱い、既に埋まっている着順スロットは
    (単勝・馬単からの)より情報量の少ないデータで上書きしない。三連複・馬連・ワイド等は
    どの馬が何着かという順序までは分からない(組み合わせの中の順不同)ため着順推測は行わない
  - 払戻レート: 全式別が対象。同じレース・式別・組み合わせに複数回の取込で異なるレートが
    来た場合は最新の値で上書きする
- **確定扱いの統一**: Club JRA-Net購入履歴CSVは既に決着済みの購入履歴であり
  「未確定(結果待ち)」は存在しないという前提のもと、**「的中／返還」列の値の有無にかかわらず、
  常に払戻(`payout`)を確定させる**。的中買い目は的中組み合わせごとの払戻レート(またはrefund)、
  それ以外(的中／返還列が完全に空の行を含む)は0円を`payout`として保存する
  (`payout=null`になるケースはない)。あわせて、`imported_ticket_groups.status`も
  常に`'settled'`として保存する。v10より前の旧形式データ(`imported_tickets`)を後方互換表示
  するレガシー経路(`GET /api/ticket-imports`内の legacy 分岐)でも同様に、`refund`が0の行は
  `payout: 0`として返す
  - **別の未解決事項として残るもの**: 「的中／返還」列が完全に空ではなく、かつ「的中」を
    含まない文字列(出走取消等による「返還」表記等)が入っている行は、この対応の対象外
    (元々`payout=0`になっていた)。この場合、実際には返還(全額払戻・収支への影響は±0に
    近いはず)のケースを損失として計上してしまっている可能性があり、実データでの表記確認が
    必要(下記「要確認」参照)
- CSVインポート時点では馬名・騎手が分からず、購入履歴(`imported_ticket_items.selections`)には
  馬番のみが入る。レース管理画面で出走馬表(馬名・騎手)を登録・更新すると、同じレース・馬番を
  参照している`imported_ticket_items`および`tickets`の`selections`へ馬名・騎手を書き戻す
  (バックフィルする、`functions/api/_lib/entries-merge.js`の`backfillHorseNamesForRace`)。
  既に値が入っている項目は上書きしない。出走馬一覧PDFインポート(`entries-import.js`)も、
  `horse_number`が確定した(nullでなくなった)タイミングで同じ関数を呼び出しバックフィルを行う
- 未登録レースは新規作成し、出走馬は空配列のままでもよい
- CSV原本は `imported_tickets` に残し続ける(正規化後も削除しない)
- 既存CSVデータも履歴APIで後方互換表示する
- 同一購入の自動統合は行わない
- 日付・受付番号がともに空の行(CSV末尾の「合計」行など)は購入データではないためスキップする
- 重複判定キー(`receipt_number`+`sequence_number`)には`race_date`も含める(受付番号は
  開催日ごとに採番がリセットされることがあるため)
- 「的中／返還」列に複数の的中組み合わせが含まれる行(BOX・ながし等で複数点的中した場合、
  例: `的中02―05／的中02―09／的中05―09`)は、組み合わせごとに分解して個別に払戻額を計算する。
  払戻額は「払戻単価」列(100円あたりのレート、こちらも同じ並び順で／連結される)を
  組み合わせごとの購入金額に乗じて算出する(`payout = round(rate / 100 * amount)`)
- レース削除時は、そのレースに紐づく `imported_ticket_groups`／`imported_ticket_items` に加えて、
  参照元の `imported_tickets`(CSV原本)も削除する(孤立データ化・再取込不能を防ぐため)
- **実装上の注意**: `functions/api/ticket-imports/index.js`の`GET`/`POST`は、関連テーブルを
  1回のクエリでまとめて取得しメモリ上でグループ化する、既存重複チェック用のキーと
  レース参照をリクエスト単位でキャッシュする、重複候補検出は全行処理後に1回だけ行う、
  という方式でCloudflare Workersの1リクエストあたりのサブリクエスト数上限を回避している。
  この関数(および同様に「1件ごとにDBへ問い合わせるループ」を含む実装)を今後変更・追加する
  際は、同じ問題を再発させないよう注意すること(`functions/api/races/entries-import.js`・
  `functions/api/races/results-import.js`も同じ方式へ書き直し済み。上記「実装上の注意
  (サブリクエスト数対策)」参照)
- **集計への反映漏れ(未確認)**: `GET /api/ticket-imports`が返す各アイテムには
  `race_finish_order`/`race_payouts`(通常購入`tickets`側にはJOINで付与されているレース確定
  情報)が含まれていない。的中率集計への影響は`docs/BACKLOG.md`参照
- `imported_ticket_groups`の一意制約(`uq_imported_group_source_key`、`UNIQUE(source,
  group_key)`)には`user_id`を含めていない(同じJRA-Netアカウントを複数のアプリユーザーが
  共有する想定をしていないため)。万が一衝突した場合、`imported_ticket_groups`への
  INSERT箇所でこの制約違反だけを検知し、該当行を「conflicted」として数えて`conflicts`配列
  (該当行の日付・競馬場・レース番号・式別・買い目と理由メッセージ)へ積み、他の行の取込処理は
  継続する。レスポンスの`conflicted`件数・`conflicts`配列を購入履歴画面(`app.js`)の
  CSVインポート結果アラートに表示する

### 「馬／組番」列の解析

CSV出力元によって表記ゆれがあるため、以下を吸収して解析する。

- 馬番の区切り文字: 半角/全角ハイフン、水平線(―)、各種ダッシュ、読点、矢印(→)など
- 購入方式ごとの表現:
  - 通常・複数点(セミコロン区切りで組み合わせを列挙): そのまま個別買い目として扱う
  - BOX(スラッシュなし・全馬番の羅列): 全馬番から式別の点数分のC(n,k)を生成
  - ながし・軸1頭/2頭(全角スラッシュ2群、軸＋相手群): 相手群から不足分を選び軸と組み合わせる
  - フォーメーション(全角スラッシュの群数が式別の点数と一致): 各群から1頭ずつ選ぶ直積
  - マルチ・馬単/三連単等の着順ありの式別: 生成した組み合わせの全順列を購入点数として扱う
- 購入金額列が「単価／合計」の複合表記(例: `200／3600`)の場合は合計(末尾の値)を採用する

