# 未着手タスク・引継ぎメモ

このファイルは、要望としては承認済みだが未実装のタスクを引き継ぐための一覧です。
`README.md` / `docs/DESIGN.md` / `docs/TESTING.md` とは異なり「現状」ではなく
「これから対応すること」を記録する場所です。着手したら該当タスクをここから削除し、
`docs/DESIGN.md`(必要なら`docs/TESTING.md`も)に現状として反映してください。

完了済みタスクの詳細な作業経緯(フェーズ1〜15)は`archive/documents/BACKLOG_HISTORY.md`
に退避してあります。新しいセッションで同じ修正を重複して行わないための参考としては
そちらを、次に何をやるかの判断には本ファイルを使ってください。

## 🔰 次のチャットで最初に読むこと(引き継ぎサマリー)

- アプリの全体像は`README.md`、データ構造・仕様の詳細は`docs/DESIGN.md`、
  手動テスト項目は`docs/TESTING.md`を参照(いずれも現状を反映した生きたドキュメント)。
  過去の作業経緯は`archive/documents/BACKLOG_HISTORY.md`参照
- 作業の進め方は毎回同じパターン: **①要望の仕様矛盾・不足を確認→承認を得る
  →②`docs/DESIGN.md`等を仕様に合わせて更新→③実装→④`node --check`で構文チェック
  →⑤`docs/BACKLOG.md`のタスク表・完了履歴を更新→⑥zipに固めてダウンロード提供**
- 実機(wrangler dev / 本番)での動作確認は毎回**未実施**。コードレビューと
  Node上でのロジック単体テスト・シミュレーションのみで検証している。
  実機確認はユーザー側で行ってもらっている状況
- **2026-08-09: 「JRAレース結果PDFをインポート」の0レース問題(2026-08-06修正・
  パーサー`8.0.0-gapjoin`)、および実機登録内容確認で判明した①騎手が未登録・
  ②出走馬が着順順に並ぶ、は実機で解消を確認済み(①は実機で騎手名の正常表示を
  確認、②は実装済み)。③枠番と馬番が一致しない、は実機で試したところ
  「枠番の算出方向が実際のJRAの傾向と逆(余りが1・2枠側に寄ってしまう。
  本来は7・8枠側に寄るべき)」と追加で判明し、原因は`public/races.js`の
  `defaultWakuNumber()`自体のロジック誤りだった。これを修正した(Node単体
  テストで9・11・16・18頭のケースを検算し、実際のJRAの傾向と一致することを
  確認済み。実機再検証は未実施)。この関数はPDFインポートの③修正だけでなく
  **通常のレース新規登録時の枠番自動設定機能とも共通**のため、今回の修正で
  両方が正しくなる。詳細は
  `docs/JRA_RESULT_PDF_IMPORT_PAYOUT_ISSUE_INVESTIGATION.md`参照(パーサー
  `8.1.0-jockey-order-waku`のまま。枠番ロジックの修正は`races.js`側のため
  パーサーバージョンは据え置き)。
  - **次のセッションでは、まず実機でPDFインポートを再実行し、③(枠番)が
    正しく反映されているか確認すること。あわせてレース新規登録画面でも
    頭数選択時の枠番初期値が正しい方向に寄るか確認すること**
  - **④ 払戻の一部欠落(複勝1・3着、枠連、ワイド1-2/2-3着)のみ未解決**。
    単体テストではロジック自体に問題がないことを確認済みのため、実機の
    診断パネル「PDF全文抽出テキストを表示」の該当箇所(複勝・枠連・ワイドの
    払戻表部分)を確認できるまで保留
  - なお、`results-import.js`の払戻マージが「既存が空の場合のみ」しか反映
    しない不具合(a)、PDF側の払戻抽出が矢印区切り(→)に未対応の可能性(b)は
    引き続き**未修正**のまま(下表参照。別途承認を得てから対応する)**
- 2026-08-04: `docs/BACKLOG.md`を整理した。①完了済みの作業経緯(フェーズ1〜15、
  複数ユーザー対応の確定仕様、リファクタリング候補1・3の詳細)を
  `archive/documents/BACKLOG_HISTORY.md`へ退避してファイルを軽量化。②未着手タスクを
  優先度・規模の単純な表から、**触るファイル/画面が同じタスクをまとめたクラスタ単位**の
  分類に組み替えた。粒度がバラバラだった項目(例: 「レース管理画面の細かいUI改善」6件)は
  同一クラスタにまとめ、逆に規模が大きく複数の関心事が混ざっていた項目は分割候補として
  注記した。**優先順位そのものの見直しはまだ行っていない**(クラスタ分類の完了後、
  次のチャットでユーザーと相談して決める想定)
- 2026-08-04: クラスタ2(馬券履歴画面)の「カレンダー日別収支サマリをK表記→実数表示」を
  実装済み。`public/app.js`の`renderHistoryCalendar`内、`Math.round(profit/1000)}k`を
  `profit.toLocaleString()`に変更。桁数増加に伴い`public/style.css`の`.calendar-day-money`の
  フォントサイズを縮小(700px以下は`.6rem`、430px以下は`.52rem`)。`docs/DESIGN.md`
  「画面仕様」に反映済み
- 2026-08-04: クラスタ2の「同日内の履歴を競馬場・レース番号の昇順でソート」を実装済み。
  `public/app.js`の`groupByRace`の並び替えを、日付降順のみ→日付降順・同日内は競馬場名
  (ロケール比較)・レース番号(数値)の昇順の複合ソートに変更。`docs/DESIGN.md`
  「画面仕様」に反映済み
- 2026-08-04: クラスタ2の「履歴削除直後に一覧からすぐ消えるようにする」を実装済み。
  `public/app.js`の削除成功時の`alert("購入を削除しました。")`(ブロッキング)を廃止し、
  `loadTickets()`を即座に呼ぶよう変更。失敗時のエラー`alert()`は維持。`docs/DESIGN.md`
  「画面仕様」に反映済み
- 2026-08-04: クラスタ2の「履歴カレンダーの『今日』ボタンで日付も選択状態にする」を
  実装済み。`public/app.js`の`history-today-btn`のクリックハンドラで、表示月の切り替えに
  加えて`selectedHistoryDate`を今日の日付にセットし`applyHistoryFilter()`を呼ぶよう変更。
  `docs/DESIGN.md`「画面仕様」に反映済み
- 2026-08-04: クラスタ1の「日付選択後の一覧で『未登録』行クリック時、競馬場・レース番号を
  事前入力してレース登録モーダルを開く」を実装済み。`public/races.js`の`.no-race`枠
  (管理者のみ)にクリックハンドラを追加し、`openModal(null, { race_date, track,
  race_number })`を呼ぶよう変更。`openModal()`の`prefill`引数は元々admin.htmlの
  「未登録レース一覧」からの遷移向けに用意されていたものを流用した。`docs/DESIGN.md`
  「画面仕様」に反映済み(のちに下記の通りモーダル分割で`openEntriesModal()`へ改名)
- 2026-08-04: クラスタ1(races.js/races.html)の残り5件をすべて実装済み。①「テキストから
  出走馬を一括入力」ボタンの横に「保存する」ボタンを複製配置(`race-entries-form`内、
  `.paste-box-actions`)。②レース一覧カードの編集ボタン(✎)1つを廃止し、出走馬登録状況
  バッジ・結果確定状況バッジ自体を管理者向けボタン化して「出走馬表を登録/編集」「払戻を
  登録/編集」に分割、レース登録・編集モーダルも`race-entries-modal`(出走馬表)・
  `race-payout-modal`(払戻)の2つに完全分割した(ユーザー承認済みの方針)。
  `index.html`の`races.html?edit=`リンクは払戻モーダルを直接開くよう対応。③払戻入力欄
  (式別ブロック・組み合わせ行)をCSS Gridでモーダル幅に応じて横並び表示にし、縦スクロール
  量を削減。④カレンダーで日付未選択時、一覧を「表示中の月のレースのみ」に絞り込むよう変更
  (専用の「選択解除」ボタンは廃止。同じ日付を再クリックすると解除される既存動作を活用)。
  ⑤出走馬表・払戻の各モーダルを開くたびに`.modal`要素の`scrollTop`を0にリセットし、前回
  開いたレースのスクロール位置が残らないようにした。`docs/DESIGN.md`「画面仕様」に反映済み。
  クラスタ1はこれで全件完了のため本ファイルの表から削除

依存関係と「同じファイル/画面を触るのでまとめて着手すると効率的」という観点で
クラスタに分類したもの。各クラスタ内は元の優先度(高/中/低)を維持している。
**クラスタ間の優先順位はまだ決めていない**。次にやるものを選ぶ際は、まずどの
クラスタから着手するかを相談してから、クラスタ内の並び順を決めるとよい。

## ⚠️ 調査中の不具合(未解決・修正未承認)

着手中/報告済みだが原因確定・修正がまだのもの。上の「クラスタ」とは別枠で管理する
(新機能追加ではなく、既存機能の不具合調査のため)。

| 状態 | 内容 | 詳細 |
|---|---|---|
| 🟢 0レース問題は解消確認済み。細部(騎手・並び順・枠番)も修正済み | JRAレース結果PDFインポート(払戻取込)。0レース問題は`jraResultJoinRowItems`(ギャップ実測方式)で解消(実機で headers=12 records=12 を確認済み)。その後判明した①騎手未登録・②出走馬が着順順・③枠番と馬番の不一致もすべて修正済み(Node単体テスト・構文チェック済み)。残るは④のみ | `docs/JRA_RESULT_PDF_IMPORT_PAYOUT_ISSUE_INVESTIGATION.md` |
| 🟡 未修正・追加情報待ち | ④ 払戻の一部が取得できていない(複勝1・3着、枠連、ワイド1-2/2-3着)。単体テストではロジック自体に問題なく取得できることを確認済みのため、実機の生テキストとの食い違いが原因と推測。次回インポート時の診断パネル「PDF全文抽出テキストを表示」の該当箇所(複勝・枠連・ワイドの払戻表部分)が必要 | 同上ドキュメント内④ |
| 🟡 発見済み・未修正(0レース問題とは別件・今回の修正範囲外) | `functions/api/races/results-import.js`の払戻・着順マージが「既存が完全に空の場合のみ」しか反映しない設計になっており、CSVインポート等で一部式別だけ既に登録されている場合、PDFインポートの新しい結果が反映されない | 同上ドキュメント内(a) |
| 🟡 発見済み・未検証(0レース問題とは別件・今回の修正範囲外) | PDF側の払戻金額抽出正規表現が組み合わせの矢印区切り(→)に対応していない可能性(馬単・三連単で該当しうる) | 同上ドキュメント内(b) |

### クラスタ2: 馬券履歴画面(app.js)の小改善まとめ
同じくapp.js内で完結する小規模改善がまとまっている。「複数選択まとめて削除」のみ
新規API新設が必要で規模が一段大きいため、まとめて着手する場合も最後に回すか、
別セッションに切り出すことを検討する。

| 優先度 | 内容 | トークン使用量目安 |
|---|---|---|
| 中 | レースカードの表示順を「日付・競馬場・レース番号(左)」「購入額・払戻額・収支(右)」に変更 | 小〜中 |
| 中 | カレンダーの「今日」判定・日付切り替えをJST基準にする | 小〜中 |
| 中 | **(規模が異なるため切り出し候補)** 複数の履歴を選択してまとめて削除できる機能。一括削除用APIの新設が必要(tickets/ticket-importsでエンドポイントが異なる点に注意) | 中〜大 |

**クラスタ合計目安**: 小〜中の2件は**小**程度、一括削除(中〜大)を含めると**中〜大**

### クラスタ3: 馬券購入画面(buy.js)の改善
コース・距離のスキーマ拡張(実装済み)を前提とする2件と、購入フローに関わる2件。
「馬券かご」機能は同じbuy.jsを触るが購入フロー自体を作り替える独立した大型機能のため、
別クラスタ(クラスタ8)として扱う。

| 優先度 | 内容 | トークン使用量目安 |
|---|---|---|
| 中 | レース一覧の1行の情報を「レース番号・レース名・芝/ダート/障害・距離・頭数」にする | 中 |
| 中 | カレンダーで日付選択後のレース一覧で、購入済みレースが見た目で分かるようにする(`GET /api/tickets`との突合が必要) | 中 |
| 低 | 購入モーダルの初期表示を「単勝が選択され、馬選択画面まで表示された状態」にする | 小 |

**クラスタ合計目安**: 3件まとめて**中**程度

### クラスタ4: 集計画面(stats.js)
2件目は1件目(スキーマ拡張)完了が前提。規模が大きく異なるため同時着手は非推奨、
まず4階層化を片付けてからコース別集計に進む方が段階的でリスクが低い。

| 優先度 | 内容 | トークン使用量目安 |
|---|---|---|
| 中 | レース別収支を「年月→日付→競馬場→レース」の4階層アコーディオン表示にする | 中〜大 |
| 高 | 競馬場別の収支を、コース別(芝/ダート/障害・距離)の収支に変更。コース情報が無いレースは競馬場全体に集約するフォールバック設計 | 大 |

**クラスタ合計目安**: 2件合計で**大**(4階層化を先に片付けるのを推奨)

### クラスタ5: 予想登録画面(prediction.js)
| 優先度 | 内容 | トークン使用量目安 |
|---|---|---|
| 中 | 各馬にメモがあるかどうかを、アコーディオンを開かなくても分かるようにする(既存の`.horse-note-preview`が目立たない問題への対応) | 小 |
| 中 | 「このレースの購入馬券」欄(馬連ボックス・ワイドボックス等のグループカード)を開閉可能にする(現状は常時全展開) | 小〜中 |

**クラスタ合計目安**: 2件まとめて**小〜中**

### クラスタ6: 管理画面・権限表示(admin.js/admin.html + 各画面のnav)
2件目は「レース管理画面へ画面遷移せず完結させたい」という要望で、races.js側(出走馬表
モーダル・払戻モーダル)とも関係するが、変更の起点はadmin.html側の設計判断
(共通コンポーネント化 or 個別実装)になるため本クラスタに置く。

| 優先度 | 内容 | トークン使用量目安 |
|---|---|---|
| 中 | 一般ユーザーの上部ナビに「レース管理」タブを表示しない(`data-admin-only`の仕組みを流用)。**2026-08-07: ユーザーから改めて明示的に要望あり(優先度変更なし)。一般ユーザーは「馬券購入・馬券履歴・集計」の3タブのみでよく、管理系メニューは一切不要とのこと。対応時は`races.html`のnavリンク(`<a href="races.html" class="nav-link">`)にも`data-admin-only`を付与し、他画面のnavと同様の出し分けにする** | 小 |
| 低 | 登録ユーザー一覧の「登録日時」表示をJST(日本時間)にする | 小 |
| 中 | 「未登録レース一覧」からレース情報登録まで画面遷移せず完結できるようにする(モーダル共通化 or admin.html側に個別実装。設計方針は着手時に要相談) | 大 |
| 中 | **(2026-08-07追加)** iPhone SE3等の小型スマホで、上部ナビ(masthead-nav)のタブが2行に折り返されてしまう。`public/style.css`の`@media(max-width:700px)`には既に`.masthead-nav { overflow-x:auto; white-space:nowrap; }`が設定済みで、本来は横スクロールする想定になっているが、実機では折り返しが発生している。**単純な幅詰め(見た目調整)だけでなく、想定通りにスクロールが効いていない原因(親要素の`flex-wrap`の影響、`.masthead`側の`flex-wrap:wrap`との競合等)をあわせて調査すること**。`.masthead`は`flex-wrap: wrap`が指定されており、`.masthead-inner`(タイトル+nav)自体が折り返し対象になっている可能性がある | 小〜中 |

**クラスタ合計目安**: 小さい3件は**小〜中**、画面遷移統合(大)を含めると**大**

### クラスタ7: CSVインポート仕様変更
| 優先度 | 内容 | トークン使用量目安 |
|---|---|---|
| 中 | 取り込んだ結果は外れ馬券も含めすべて「確定」扱いにする(`hitText===''`の場合もpayout=0にする) | 小〜中 |

**クラスタ合計目安**: 1件のみ・**小〜中**

### クラスタ8: 馬券かご機能(独立した大型機能)
仕様確定済み・単独で着手可能。詳細は下記「馬券かご 確定仕様」参照。

| 優先度 | 内容 | トークン使用量目安 |
|---|---|---|
| 高 | netkeiba・楽天競馬のような「馬券かご」機能(複数の買い目をかごに入れてまとめて購入・保存) | 大 |

**クラスタ合計目安**: 1件のみだが仕様が大きく**大**

### クラスタ9: レース結果一括登録(サンプル受領済み・外部データ連携とは別扱い)
下のクラスタ10(出走情報・払戻情報の自動取得)とは異なり、**外部サイトへのアクセスを
伴わない**(ユーザーが手動でコピーしたテキストを貼り付ける方式)ため、bot対策等の
技術的な制約は無く、現時点で着手可能。

**2026-08-08時点の状況**: 「JRA公式サイトの払戻一覧ページからコピーしたテキストを
貼り付け」という当初仕様から、実際にはPDFインポート方式(`jra-result-pdf.js`)として
実装が進んでいた。実機で0レースとなる不具合・その後判明した騎手/並び順/枠番の
不備は修正済み(実機で12レース登録まで確認済み)。**払戻の一部欠落(④)のみ未解決**
のため、このタスクを完了扱いにはしていない。次に着手する際はまず④の解決を優先すること。

| 優先度 | 内容 | トークン使用量目安 |
|---|---|---|
| 高 | JRA公式サイトの払戻一覧ページからコピーしたテキストを貼り付けると、該当日・競馬場の全レース分の着順・払戻を一括登録できるようにする。将来的には1Rのみ等レース単位でも対応予定 | 大 |

**クラスタ合計目安**: 1件のみ・パーサー設計から必要なため**大**

### クラスタ10: 外部データ自動取得(保留)
JV-Link(Windows専用ActiveX)・JRA公式サイト(bot対策あり)・netkeiba(規約違反)の
いずれも技術的/規約的な制約があり保留中。詳細は下記「出走情報・払戻情報の自動取得に
ついて(調査結果)」参照。方針転換する場合のみ再検討する。

| 優先度 | 内容 | トークン使用量目安 |
|---|---|---|
| 中(保留) | レース登録画面に「出走情報取得」ボタン(枠番・馬番・馬名・騎手名・種牡馬名を自動反映) | 大 |
| 中(保留) | レース登録画面に「払戻情報取得」ボタン(着順・払戻を自動反映) | 大 |

**クラスタ合計目安**: 保留中のため着手予定なし(仮に着手する場合は2件で**大**)

### クラスタ11: リファクタリング候補
「実装がツギハギな気がする」との指摘を受けて2026-08-04に調査したもの。候補1・3は
実装済み(詳細は`archive/documents/BACKLOG_HISTORY.md`参照)。候補2・4・5は未着手。
着手する場合はユーザーに優先順位を確認してから進めること。

### 候補2(小〜中): `formatDate`の重複整理
- `formatDate`が`admin.js`・`prediction.js`・`races.js`・`stats.js`・`utils.js`の5箇所に存在し、
  **出力形式がそれぞれ異なる**(年あり/なし、曜日あり/なし、`admin.js`のみ独自の
  `日月火水木金土`配列を使う等)。単純に1つへ統合すると各画面の表示が変わってしまうため注意
- **対応方針**: `utils.js`にオプション引数(年/曜日の有無等)を持たせるか、用途別に
  `formatDateShort`/`formatDateWithWeekday`等の複数関数を用意して`utils.js`に集約し、
  各画面は現状と同じ見た目になるよう対応する関数を呼び出す形に変更する
- 着手前に各画面の現在の表示例をスクリーンショット等で記録しておくと、統合後の
  見た目比較がしやすい
- リスク: 中(表示形式の回帰に注意。自動テストが無いため目視確認が必要)

### 候補4(大): `buy.js`(719行)・`races.js`(708行)の巨大ファイル分割
- 状態管理(モジュールレベルのミュータブル変数)・API呼び出し・DOM描画・イベント登録が
  1ファイルに混在しており、「ツギハギ」感の主な原因とみられる
- 本アプリの中核機能(馬券購入・レース管理)であり、自動テストが無く実機確認もユーザー側
  任せの運用(本ファイル冒頭参照)のため、壊れた場合の影響が大きい
- **対応方針(着手する場合)**: 一度に全体を作り直すのではなく、関心事(state/api呼び出し/
  描画関数)ごとに段階的に切り出す。着手前に分割方針(ファイル構成案)をユーザーに提示して
  承認を得ること
- リスク: 高

### 候補5(大・保留推奨): CSSキャッシュバスティングの一元化
- 現状、`style.css`変更のたびに全6HTMLファイルの`?v=YYYYMMDDHHmm`クエリパラメータを
  手作業で書き換える運用(`docs/DESIGN.md`「CSSのキャッシュ対策」参照)
- 根本対応にはビルドツール導入が必要になるが、本プロジェクトは「Node不要・wranglerのみで
  手動運用」という明確な方針(README/DESIGN.md「DBマイグレーションの運用」等で繰り返し
  言及)を掲げており、静的サイト全体をビルド前提にするのは方針と衝突する
- 軽量な折衷案(1箇所のバージョン文字列をJSで参照し各HTMLの`<link>`へ動的付与する等)は
  あり得るが、費用対効果は低いと判断
- **今回のリファクタリング対象からは除外し、着手しない方針を推奨**(ユーザーの意向次第で
  将来的に再検討)

## 参考: 今回確認した仕様矛盾の解消方針

- 予想印「1頭1つまで」は反映済み(`docs/DESIGN.md`・`docs/TESTING.md`とも更新済み)
- 「レース確定後は購入不可」は一時実装したが2026-07-30に撤回し、確定後も購入可能な方針で反映済み(`docs/DESIGN.md`「ロック仕様」参照)
- 「確定後のチケット編集・削除禁止(ロック)」は2026-07-31に全面撤廃済み(`docs/DESIGN.md`「ロック仕様」参照)

## 出走情報・払戻情報の自動取得について(調査結果・2026-08-01)

「レース登録画面/払戻入力画面に取得ボタンを置き、開いているレース1件分だけをその場で取得する」という
UI案が出たが、1件ずつであっても呼び出せるHTTP経由の取得口がそもそも存在しないため、現状は保留とする。

- **JRA-VAN Data Lab.**(公式・月額2,090円): 唯一のインターフェースが「JV-Link」というWindows専用の
  ActiveX COMコンポーネント。Cloudflare Pages Functions(サーバーレスのJavaScript実行環境)からは
  技術的に呼び出せない。公式ドキュメント上もWindows Serverでの動作は非対応とされている
- **JRA公式サイト(jra.go.jp)への直接アクセス**: 実際にfetchで試したところ、bot対策により
  即座にブロックされることを確認した。利用規約の文言以前に、技術的にアクセスできない
- **netkeiba**: 利用規約でスクレイピングを明示的に禁止している(2024年以降Bot対策も強化)

選択肢(いずれも保留中。方針転換する場合は下記から選ぶ):

- A. 貼り付け方式を磨く方向で進める(現状維持・推奨)
- B. 有料のスクレイピング代行API(Bright Data等)を利用する。技術的には可能だが月額コストが発生し、
  対象サイトの規約違反リスクが完全になくなるわけではない
- C. Windows常時稼働機を別途用意し、JV-Linkを使った中継サーバーを立てる(JRA-VAN公式のため
  規約面は最もクリーンだが、インフラ運用の手間が大きい)

## 馬券かご 確定仕様(2026-08-01)

目的: 複数レース・複数式別にまたがる買い目を、都度保存せずに一時的にためておき、
最後にまとめて保存する。netkeiba・楽天競馬の「馬券かご」相当。

確定した仕様:

- 対象範囲は「レースをまたいでよい」。複数レース・複数式別の買い目を1つのかごに貯められる
- 現行の「式別を選んで組み合わせ選択→即保存」という都度保存の購入フローは廃止し、
  「かごに追加」→(必要なだけ繰り返す)→「まとめて購入する」の流れに一本化する
  (1点だけ買いたい場合も、必ずかごを経由する)
- かごの中身はブラウザ/端末を閉じたり、後日開き直しても保持されている必要がある。
  → サーバー保存(下書きテーブル)までは不要と判断し、localStorageに保存する方式とする
    (同一端末・同一ブラウザ内でのみ保持され、他の端末とは同期されない想定。複数端末での
    同期が必要になった場合は、サーバー側に下書き用テーブルを追加する改修が別途必要になる)
- 「まとめて購入する」実行時は、かご内の全買い目を1回のAPIリクエストで送信し、サーバー側は
  db.batch()でまとめて書き込む(フェーズ12の教訓を踏まえ、行ごとに個別リクエストを
  ループする実装にはしない)

画面イメージ:

- 購入画面(buy.html)に「かご」パネルを常設(右側 or 下部)。ヘッダー等にも
  「かご: n点 / 合計¥xxx」のように現在の内容を常時表示する
- かご内の各行: 日付・競馬場・レース番号・式別・買い目内容・金額・削除ボタン
- かご下部: 合計点数・合計金額、「まとめて購入する」ボタン、「かごを空にする」ボタン
- 保存成功後はかごを空にし(localStorageもクリア)、購入履歴に反映する

実装時の主な変更点(見積り):

- buy.js: 「購入する」ボタンを「かごに追加」ボタンに置き換え、かご状態(localStorage)の
  読み書き・かごパネルの描画・合計計算を追加。レース遷移時もかごの内容を保持する
- functions/api/tickets/bulk.js(または同等の一括保存API): 複数レース・複数式別が混在した
  買い目配列を受け取れるよう、リクエスト形式を拡張(現状は1レース分の想定になっていないか要確認)
- 将来の複数ユーザー対応(下記)と組み合わせる場合、localStorageのキーをログインユーザーごとに
  分ける(同じ端末を家族で共用した際にかごが混ざらないようにする)配慮が必要
