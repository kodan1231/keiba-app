# 未着手タスク・調査中の不具合 引継ぎメモ

**FIX ver1.0**(2026-08-10整理。2026-08-11: クラスタL追加。2026-08-11(2): クラスタLを
race_results新設・レース条件詳細カラムを含む形に拡張、クラスタM新設。
2026-08-12:
FIX ver1.0完了後のコードレビューで発見した不具合3件をクラスタNとして追加)
2026-08-12: クラスタL実装完了)

このファイルは、要望としては承認済みだが未実装のタスク、および調査中・未解決の不具合を
引き継ぐための一覧です。`README.md` / `docs/DESIGN.md` / `docs/TESTING.md`とは異なり
「現状」ではなく「これから対応すること」を記録する場所です。着手したら該当タスクを
ここから削除し、他の生きたドキュメントへ反映してください。

セッションごとの詳細な作業経緯・完了済みタスクの経緯は`archive/documents/`配下
(`BACKLOG_HISTORY.md`、`pre-fix-v1.0/BACKLOG.md`)に退避してあります。新しいセッションで
同じ修正を重複して行わないための参考としてはそちらを、次に何をやるかの判断には本ファイルを
使ってください。

## 進め方の基本パターン

- ①要望の仕様矛盾・不足を確認→承認を得る → ②`docs/DESIGN.md`等を仕様に合わせて更新
  → ③実装 → ④構文チェック(`node --check`等) → ⑤`docs/BACKLOG.md`のタスク表を更新
  → ⑥zipに固めてダウンロード提供
- 実機(wrangler dev / 本番)での動作確認は基本的に未実施。コードレビューとNode上での
  ロジック単体テスト・シミュレーションのみで検証しており、実機確認はユーザー側で
  行ってもらう運用になっている。実機検証が必要な項目は各タスク・`docs/TESTING.md`に
  明記してあるので、着手・完了報告の際は検証状況を明確にすること

## 🔰 次のチャットで最初に読むこと(2026-08-12引き継ぎ)

**クラスタL(下記)の実装が完了しました。** 出走馬一覧PDFインポート・JRAレース結果PDF
インポートについて、木曜(枠番未確定)→金曜(枠番確定)→土日(結果確定)という運用フローに
加え、以下を実装済みです(`docs/DESIGN.md`「レース結果の詳細記録(race_results)」
「レース条件の詳細カラム」参照)。

- `races`テーブルへレース条件詳細カラム(`weight_type`/`class_flags`/`course_direction`/
  `weather`/`track_condition`)を追加
- `races.entries`へ`sex_age`(性齢)・`weight_carried`(負担重量)を追加(出走馬一覧PDF時点の
  予定値)
- 新テーブル`race_results`(馬単位の確定結果を1頭1行で記録: 全着順・タイム・着差・
  コーナー通過順位・推定上り・馬体重・単勝人気・取消/除外・競走中の出来事メモ)を新設
- 単勝オッズ・本賞金・付加賞・調教師名は対象外(意図的に保持しない)
- 降着・失格は対象外(今回は取消・除外のみ対応)
- 馬名ベース・騎手名ベースの集計参照(過去出走一覧・的中率等)は**今回のスコープ外**とし、
  クラスタM(下記)として別タスク化

**実データ(サンプルPDFをpdftotextでテキスト抽出したもの)での検証**は完了しています
(12レース中12レース検出・全着順抽出成功、取消・除外行の検出・競走中の出来事メモの
自動紐付けも確認済み)。ただし**実ブラウザのPDF.js経由での動作検証は未実施**です
(既存のパーサー全体と同様の既知の限界。下記「調査中の不具合」参照)。

**`migration.sql`の`-- @STEP: race_results_and_conditions`ブロックはまだ実DBに未適用です。**
以下の手順で適用してください(README「スキーマ変更の適用手順」も参照):

```bash
npx wrangler d1 execute keiba-yosou-db --local --file=migration.sql
# 問題なければ本番にも適用
npx wrangler d1 execute keiba-yosou-db --remote --file=migration.sql
npx wrangler d1 execute keiba-yosou-db --remote --command "INSERT INTO schema_migrations (name) VALUES ('race_results_and_conditions');"
```

適用後、`migration.sql`から該当ブロックを削除し、内容を`schema.sql`へ反映してください。

### 実装済みファイル一覧(2026-08-12)

- `functions/api/_shared.js`: `mergeEntriesByHorseName()`・`upsertRaceResults()`を新設

**FIX ver1.0完了後、コードレビュー(実行環境なしの静的レビュー)で3件の不具合を発見し、
修正方針を確定した(クラスタN、下記)。実装はまだ着手していない。** 次のセッションでは
クラスタNの3項目を優先度順(N-1→N-2→N-3)に実装することを推奨する。

クラスタL(race_results新設等)は引き続き実装未着手のまま(こちらも次点で着手候補)。

## クラスタN: FIX ver1.0後のコードレビューで発見した不具合の修正(2026-08-12仕様確定・実装待ち)

実行環境を用いない静的コードレビューにより発見。3件ともユーザーとの確認により修正方針を
確定済み。**実装(コード変更)はまだ行っていない。**

### N-1(中優先度): 出走馬表(races.js)で馬番が重複入力できてしまう

**症状**: `public/races.js`の出走馬表フォーム(`entry-form-row`)は、各行の馬番セレクトが
互いに独立しており、同じ馬番を複数行に設定できてしまう。馬番が重複すると、購入時の
組み合わせ紐付けや`computeWinningCombos`による的中判定が意図しない挙動になる可能性がある。

**発生源の切り分け(確認済み)**:
- 手入力(レース管理画面): 対応要
- PDFインポート系(`jra-entries-pdf.js`/`jra-result-pdf.js`経由): **対応不要**。
  重複が起きるとすればパーサー側の解析バグであり、今回のスコープには含めない
  (再現・報告があれば別タスクとして起票する)

**確定した修正方針(クライアント側のみ・スワップ方式)**:

`public/races.js`の出走馬表フォームで、ある行の馬番セレクト(`.e-horse-number`)の値を
変更した際、**送信前の時点で**重複が起きないよう、UIの操作性で防ぐ。

- 各行の馬番セレクトに、現在選択されている値を`dataset.horseNumber`として保持しておく
  (`renderEntryRows()`描画時に初期値をセットし、以後の変更のたびに更新する)
- ある行(行X)の馬番セレクトが新しい値`V`に変更されたとき:
  1. 他の行(行Y)の中で、現在の馬番が`V`である行を探す
  2. 見つかった場合、行Yの馬番セレクトの値を「行Xの変更前の値」に**入れ替える**
     (単純なブロック/警告ではなく、2行の値をスワップすることで「常に1〜出走頭数の
     数値が重複なく1つずつ使われている」状態を維持する。ユーザーが「1」を選んだ行を
     後から別の行でも「1」に変えた場合、元々「1」だった行は自動的に元の行が持っていた
     番号に変わる、という挙動になる)
  3. スワップ後、両方の行の`dataset.horseNumber`を更新する
- 出走頭数変更時(`horseCountSelect`のchangeイベント)の再描画(`renderEntryRows`)でも、
  同様に初期値の`dataset.horseNumber`をセットし直す

**対象外とする範囲(確認済み)**:
- 枠番(`waku_number`)は9頭以上のレースで複数馬が同じ枠を共有するのが正常な状態のため、
  重複チェック・スワップ処理の対象**外**
- サーバー側(`functions/api/races/index.js`・`[id].js`)への重複チェック追加は、
  クライアント側のスワップ方式で通常操作では重複が発生しなくなるため、**今回は追加しない**
  (API直叩き等の迂回経路への防御は、必要になった時点で別途検討する低優先度項目として
  本ファイルの「今後の検討候補」に残す)
- PDFインポート経由(`entries-import.js`のマージ結果)での重複チェックは対象外
  (上記の通り、発生すれば解析バグとして別途扱う)

**実装対象ファイル**: `public/races.js`(`renderEntryRows()`・馬番セレクトの`change`
イベントハンドラ)のみ。

### N-2(中優先度): コース種別・距離のいずれか一方のみ入力の場合、表示が欠落する

**症状**: `public/races.js`の`renderRaceRow()`(レース一覧カード)・`openPayoutModal()`
(払戻モーダルの読み取り専用情報)双方で、以下のロジックになっている。

```js
const courseText = courseLabel ? `${courseLabel}${r.distance ? r.distance + "m" : ""}` : "";
```

`course_type`が未入力(null)で`distance`のみ入力されている場合、`courseLabel`が空文字
(falsy)になり`courseText`全体が`""`になるため、**入力済みの距離が一覧・モーダルどちらにも
一切表示されない**(データ自体はDBに保存されており消えてはいないが、画面上「入力したのに
表示されない」不具合になる)。逆(距離未入力・コース種別のみ入力)は正しく表示される。

**確定した修正方針**: コース種別・距離をそれぞれ独立した要素として組み立て、**登録されて
いる方だけを表示する**(どちらか一方のみの入力でもその一方は必ず表示され、両方入力済みなら
両方表示、両方未入力なら何も表示しない)。

```js
// courseTypeShort() の近くに新設する共通関数
function formatCourseText(courseType, distance) {
  const parts = [];
  const label = courseTypeShort(courseType);
  if (label) parts.push(label);
  if (distance) parts.push(`${distance}m`);
  return parts.join("");
}
```

同じロジックが`renderRaceRow()`・`openPayoutModal()`の2箇所に重複していたのが今回の
バグの一因(片方だけ直して他方を直し忘れる事故の温床)でもあるため、上記の共通関数へ
統一し、2箇所とも`formatCourseText(r.course_type, r.distance)` /
`formatCourseText(race.course_type, race.distance)`を呼び出す形に置き換える。

**実装対象ファイル**: `public/races.js`のみ(`courseTypeShort()`の定義箇所に
`formatCourseText()`を追加し、`renderRaceRow()`・`openPayoutModal()`の該当箇所を置き換える)。

### N-3(低優先度・記録のみ): ログアウトAPIが未認証(セッション切れ)状態だと401になりCookieが消えない

**症状**: `functions/_middleware.js`の`isPublicAuthRoute`判定は`/api/auth/login`・
`/api/auth/register`のみを未認証アクセス許可の対象にしており、`/api/auth/logout`は
含まれていない。セッションCookieが既に無効(期限切れ・破損・削除済み)な状態で「退場」
ボタンを押すと、`POST /api/auth/logout`自体が(ミドルウェアにより)401で弾かれ、
Cookieクリア処理(`Set-Cookie`によるMax-Age=0)まで到達しない。

**影響度(確認済み・低)**: `authedFetch`が401を検知した時点で`login-screen`を表示する
実装になっているため、体感としては「ログアウトできている」ように見え、実害はほぼない。
古いCookieが残ったままになるだけで、次回の正常ログイン時には新しいセッションCookieで
上書きされるため実質的な問題は生じない。

**確定した対応方針**: **今回は実装せず、方針の記録のみに留める**(ユーザー確認済み)。
将来対応する場合は以下の方針で対応する。

```js
// functions/_middleware.js
const isPublicAuthRoute =
  url.pathname === "/api/auth/login" ||
  url.pathname === "/api/auth/register" ||
  url.pathname === "/api/auth/logout";
```

`logout.js`はセッション内容を一切参照せず、常にCookieクリアのレスポンスを返すだけの
処理のため、認証チェックを外しても情報漏洩・不正操作のリスクはない(誰が呼んでも
「Cookieを消す」以上のことはできない)。

### 今後の検討候補(クラスタN内・低優先度・未確定)

- N-1のサーバー側(API直叩き)向け重複チェックの要否は、実際にAPI経由での不正な
  重複データが確認された場合に改めて検討する

- `functions/api/races/entries-import.js`: 共通マージヘルパー呼び出しに置き換え、
  性齢・負担重量・レース条件詳細に対応
- `functions/api/races/results-import.js`: 共通マージヘルパー呼び出しに置き換え、
  `race_results`へのUPSERT、レース条件詳細カラムの保存を追加
- `functions/api/races/[id].js`: レース条件詳細カラムの手動更新を受付可能に
- `functions/api/races/[id]/results.js`: **新規**。`race_results`一覧取得・
  `incident_note`編集用API
- `public/jra-entries-pdf.js`: 性齢・負担重量・レース条件詳細の抽出を追加
- `public/jra-result-pdf.js`: レース条件詳細・天候/馬場状態・全着順・タイム・着差・
  コーナー通過順位・推定上り・馬体重・単勝人気・取消/除外・競走中の出来事メモの抽出を追加。
  `defaultWakuNumber()`による推定値埋め込みを廃止。見習い記号を保持するよう変更
- `public/races.js`・`public/races.html`: 払戻モーダルに`race_results`詳細の閲覧・
  `incident_note`編集UIを追加

### 未実施・今後の確認事項

- 実ブラウザのPDF.jsでの動作確認(次回実機で試す際は`docs/TESTING.md`参照)
- `docs/BACKLOG.md`「クラスタM」(馬名ベース・騎手名ベースの集計参照画面)は未着手のまま



## ⚠️ 調査中の不具合(未解決・修正未承認)

| 状態 | 内容 | 詳細 |
|---|---|---|
| 🟡 未修正(クラスタLでは対象外・別タスク) | `functions/api/races/results-import.js`の`finish_order`/`payouts`マージが「既存が完全に空の場合のみ」しか反映しない設計になっており、CSVインポート等で一部式別だけ既に登録されている場合、PDFインポートの新しい結果が反映されない。**2026-08-11時点でクラスタLの対象からは明示的に除外することが確定済み**(土日の「必ず再計算」自体は行うが、マージの反映漏れそのものは解消しない) | `docs/DESIGN.md`「JRAレース結果PDFインポート」既知の制約 |
| 🟡 未検証 | PDF側の払戻金額抽出正規表現が組み合わせの矢印区切り(→)に対応していない可能性(馬単・三連単で該当しうる) | `docs/DESIGN.md`「JRAレース結果PDFインポート」既知の制約 |
| 🟡 未修正(要サンプルCSV確認) | CSVインポートで「的中／返還」列が「的中」を含まない行(出走取消等による返還を想定)は、`payout`が一律0円(全損)として計算されている可能性がある。返還の場合は本来ほぼ全額が払い戻される(収支への影響は±0に近いはず)ため、実データでの表記を確認したうえで対応要否を判断する必要がある | `docs/DESIGN.md`「CSV取込の仕様」要確認 |
| 🟡 未修正(実害は限定的と推測・未検証) | `GET /api/ticket-imports`のレスポンスに`race_finish_order`/`race_payouts`が含まれていない。CSVインポート分の馬券のうち`payout`が未確定(null)のままレース結果が後から確定したケースで、`stats.js`の的中率集計の判定対象(分母)から漏れる可能性がある | `docs/DESIGN.md`「CSV取込の仕様」集計への反映漏れ |
| 🟡 調査中(未確証。クラスタLでは対象外・別タスク) | `jra-entries-pdf.js`(出走馬一覧PDF)で、特定の騎手(PDF内で同一文字列が繰り返し使われる場合)のみ、騎手名に調教師名が連結してしまうことがある。実ブラウザのPDF.jsが返す文字幅情報の異常が疑われるが未確証。**タブ保持・ギャップしきい値上限キャップという対策は`jra-entries-pdf.js`側には既に入っているが、`jra-result-pdf.js`側には未反映**(2026-08-11時点でクラスタLの対象からは明示的に除外することが確定済み。パーサーの非対称性の統一は別タスク) | `docs/DESIGN.md`「出走馬一覧PDFインポート」既知の制約・「JRAレース結果PDFインポート」既知の制約 |
| 🟡 未対応(今回対象外) | 降着・失格・競走中止など、取消・除外以外の着順未確定ケースは`race_results.status`で扱えない。将来`demoted`/`disqualified`等のstatus値を追加する拡張が必要 | `docs/DESIGN.md`「レース結果の詳細記録(race_results)」取消・除外の扱い |
| 🔵 実機検証未完了 | 出走馬一覧PDFインポート・JRAレース結果PDFインポートはいずれも実ブラウザのPDF.jsでの動作検証が完全には済んでいない(Node上のロジック単体テストが中心)。次回実機で試す際は`docs/TESTING.md`「1.8」「1.7.1」の該当項目、および`docs/DESIGN.md`「JRAレース結果PDFインポート」の残件を確認すること | `docs/TESTING.md`・`docs/DESIGN.md` |
| 🟢 記録のみ(修正は保留・ユーザー確認済み) | ログアウトAPI(`/api/auth/logout`)が、セッション切れ状態だと`_middleware.js`の認証チェックで401になりCookieクリアまで到達しない。実害は低い(結果的にログイン画面には戻る) | 本ファイル「クラスタN」N-3参照 |

## クラスタL: 出走馬インポート/レース結果インポートの統合修正+レース結果詳細記録の新設(2026-08-12実装完了)

出走馬一覧PDFインポート(`entries-import.js`)とJRAレース結果PDFインポート
(`results-import.js`)が同じ`races.entries`を扱うにもかかわらず異なるマージロジックを
持っていた問題、枠番の推定値がDBに保存されてしまうリスク、および「レース結果の詳細情報
(全着順・タイム・馬体重等)が記録として残らない」という不足について、ユーザーとの確認により
以下の方針が確定し、実装が完了した。**残タスクは`migration.sql`の実DBへの適用のみ
(README「スキーマ変更の適用手順」参照)。**

### 確認済みの運用フロー(前提)

- 木(entries未確定)→金(entries確定)→土日(results)が基本フローだが、**木・金のどちらか、
  または両方を省略し、土日の結果PDFインポートのみでレースが新規登録される運用もありうる**
  (ユーザーへの確認により判明。この前提が下記②③の設計判断に直結する)

### 確認済みのスコープ(対象/対象外)

**対象に含める**:
- レース情報: クラス名(既存`race_name`で対応)・斤量区分(`weight_type`)・条件フラグ
  (`class_flags`)・コース種別/回り(`course_type`既存+新規`course_direction`)・天候
  (`weather`)・馬場状態(`track_condition`。天候とは別カラム)・距離(既存`distance`)
- 出走馬情報(予定): 馬番・枠番(既存)・馬名(既存)・性齢(`entries[].sex_age`新規)・
  負担重量(`entries[].weight_carried`新規)・騎手名(既存)
- 結果情報(`race_results`新設): 馬体重・全着順(取消・除外含む)・タイム・着差・コーナー
  通過順位・推定上り・単勝人気・競走中の出来事(該当馬への注記。管理者編集可)

**対象外(意図的に除外)**:
- 本賞金・付加賞・調教師名(いずれも保存しない)
- 単勝オッズ: 結果PDFには全馬分の単勝オッズが存在しない(1着馬の払戻から近似計算できる
  だけで正式なオッズ表示ではない)ため、保存しない
- 発走時刻: 今回のスコープ外
- 降着・失格・競走中止等(取消・除外以外の着順未確定ケース)は将来対応
- 馬名ベース・騎手名ベースの集計参照API/画面(クラスタMへ)

### 実装タスク(2026-08-12実装完了。参考のため手順を残す)

1. **`schema.sql`・`migration.sql`のスキーマ変更を実際にDBへ適用する準備**
   - `migration.sql`の`-- @STEP: race_results_and_conditions`ブロック(`races`への
     カラム追加・`race_results`テーブル新設)は追記済み。以下の実装が完了してから
     `--local`→`--remote`の順で適用し、`schema_migrations`への記録、`schema.sql`への
     反映(ブロックの`migration.sql`からの削除)まで行うこと
2. **`functions/api/_shared.js`に共通マージヘルパー`mergeEntriesByHorseName()`を新設**
   - 馬名キーで1頭ずつマージ。`waku_number`/`horse_number`は既存が確定済みなら上書きせず
     「競合」として報告するのみ。`sex_age`/`weight_carried`は取込側に値があれば無条件で
     上書きする(競合検出の対象外)。`jockey`(騎手名)も同様に無条件で上書きする(騎手変更の
     可能性を考慮)。見習い減量記号(☆▲△★◇)は騎手名の先頭に残したまま保存する
   - マージ前に、既存entries側の馬名が空の項目を除去してからマージする(空行が残り続ける
     不具合の再発防止。`entries-import.js`の既存`mergeEntries()`と同じ考え方)
   - マージ後は`entries`を必ず馬番順にソートし直す(未確定の間は馬名順)
   - `race_name`・`course_type`・`distance`・`weight_type`・`class_flags`・
     `course_direction`は既存が空の場合のみ埋める。`weather`・`track_condition`は
     結果PDFにしか出現しないため、結果インポート時は常に最新の値で上書きしてよい
   - 詳細仕様は`docs/DESIGN.md`「出走馬情報(entries)のマージルールは共通」参照
3. **`functions/api/races/entries-import.js`を上記共通ヘルパー呼び出しに置き換え**
   - 既存の`mergeEntries()`関数はヘルパーへ統合するか、ヘルパーを内部で呼ぶラッパーに
     変更する(重複ロジックを残さないこと)
   - `public/jra-entries-pdf.js`側で性齢(`sex_age`)・負担重量(`weight_carried`)を
     抽出し、`entries`へ含めて送信するよう変更(騎手名/調教師名の境界判定で既に
     マッチさせている`(牡|牝|せん|セ|騸)\s*(\d{1,2})\s+[\d.]+kg`の結果を流用できる)
4. **`functions/api/races/results-import.js`のentriesマージを共通ヘルパー呼び出しに変更**
   - 現状の「既存entriesが完全に空の場合のみ丸ごと差し替え(`hasNewEntries`判定)」を廃止し、
     常に共通ヘルパーでマージするよう変更する
   - **`finish_order`/`payouts`のマージ方式(fill-emptyのみ)はこのタスクでは変更しない**
     (上記「調査中の不具合」表を参照。別タスクとして残す)
5. **`public/jra-result-pdf.js`: レース条件詳細・天候/馬場状態・全着順・タイム等の抽出を追加**
   - 発走時刻行直後の条件文から`weight_type`・`class_flags`・`course_direction`を抽出
   - `天候 晴 ダート 良`のような行から`weather`・`track_condition`を分離して抽出
   - 着順表の全行(1着〜最下位)を解析し、`race_results`送信用のレコード配列を組み立てる
     (馬番・性齢・負担重量・騎手名・タイム・着差・コーナー通過順位・推定上り・馬体重・
     増減・単勝人気)
   - `取消`/`除外`で始まる行を検出し、`status='scratched'`/`'excluded'`のレコードとして
     追加する(着順・タイム等はnull、除外は馬体重ありのケースに対応)
   - 「競走中の出来事等」以降の箇条書きから馬名を特定し、対応する`race_results`レコードの
     `incident_note`へ設定する
6. **`public/jra-result-pdf.js`: `defaultWakuNumber()`による`waku_number`推定値の
   埋め込みを廃止**
   - `jraResultParseExtractedPages()`内の、枠番が取得できなかった場合に
     `defaultWakuNumber()`で推定値を計算しその場で`entries`へ埋め込む処理を削除し、
     取得できない場合は`null`のまま送信するよう変更する(`race_results.waku_number`も
     常にnull送信)
   - 理由は`docs/DESIGN.md`「枠番の推定値をDBに保存しない」参照。表示用の目安値は
     `races.js`側の既存ロジックが引き続き担う
7. **`public/jra-result-pdf.js`: `jraResultCleanJockeyName()`で見習い記号
   (▲△☆◇)を除去せず残すよう修正**
   - 出走馬一覧PDFインポート側の表記(記号を残す)と揃える
8. **`functions/api/races/results-import.js`: `race_results`へのUPSERT処理を追加**
   - 解析結果(finished/scratched/excluded)を`race_id`+`horse_number`で`INSERT ...
     ON CONFLICT DO UPDATE`する
   - `incident_note`は、既存値が空、または直前の自動転記内容と完全一致する場合のみ
     上書きする(管理者の手動加筆を保護するため)
   - `races`のレース条件詳細カラム(`weight_type`/`class_flags`/`course_direction`/
     `weather`/`track_condition`)の保存も追加する
9. **`functions/api/races/entries-import.js`から`recomputeTicketPayoutsForRace()`を
   保険として呼び出す**
   - entries更新後、そのレースの`race_id`に対して呼び出す(`finish_order`/`payouts`が
     未確定であれば何もしないため無害)
10. **レース削除時に`race_results`も連動して削除されることを確認**
    - `race_results.race_id`は`ON DELETE CASCADE`のため、既存の`DELETE /api/races/:id`の
      実装変更は基本的に不要だが、実装後に動作確認すること
11. **レース管理画面(`races.js`)に`incident_note`の編集UIを追加**
    - 配置場所は実装時に検討(例: 払戻モーダル内、または新設の「結果詳細」表示エリア)。
      最低限、管理者が`race_results.incident_note`を閲覧・編集できればよい(今回は
      全着順・タイム等の一覧表示UIまでは必須としない。必要なら実装時に相談)
12. **実装完了後、`docs/DESIGN.md`の「実装待ち」注記・呼び出し元一覧を確定版に更新すること**
    - 特に「払戻確定時のticket反映」節の呼び出し元一覧、「出走馬インポート・レース結果
      インポートの運用フロー」節冒頭の注記を、実装後の実態に合わせて書き換える
13. **構文チェック(`node --check`)を実施し、変更ファイルをzip化して提供する**
14. **上記1のスキーマ適用手順をREADME/DESIGN.mdの記述通りに実施する案内を添える**
    (実際の適用はユーザー側の`wrangler`操作のため、Claudeが代行はしない)

### このタスクの対象外(別タスクとしてBACKLOGに残す)

- `finish_order`/`payouts`のマージが「既存が完全に空の場合のみ」しか反映しない問題
  (上記「調査中の不具合」表参照)
- パーサー(`jra-entries-pdf.js`/`jra-result-pdf.js`)の非対称性(タブ保持による区切り
  判定の精度・ギャップ実測しきい値の上限キャップの有無)の統一(上記「調査中の不具合」表参照)
- 降着・失格・競走中止など、取消・除外以外の特殊な着順未確定ケースへの対応
- 馬名ベース・騎手名ベースの集計参照API/画面 → クラスタM(下記)

## クラスタM: race_resultsを使った集計・参照画面(2026-08-11(2)追加・クラスタL完了後に着手)

`race_results`(クラスタLで新設)にデータが貯まった後、それを参照・集計する画面を追加する
タスク。**クラスタLの実装完了(実際にPDFインポートで`race_results`にデータが入るように
なった後)に着手すること。**

### 馬名ベースの集計

`horse_notes`(既存の馬メモ機能)に加えて、以下を馬名をキーに横断表示する:

- 過去出走レース一覧: 出走レース名・斤量区分・条件フラグ・コース種別/回り・天候・距離・
  枠番・馬番・騎手・斤量(負担重量)・馬体重・単勝人気・着順・タイム・コーナー通過順位・
  推定上り
- 表示場所は要検討(例: `prediction.html`の馬メモ欄近く、または専用の馬名検索画面)

### 騎手名ベースの集計

- 競馬場・コース種別ごとの単勝率・連対率(2着以内率)・複勝率(3着以内率)を算出する
- `race_results`(着順)と`races`(競馬場・コース種別)をJOINして集計する。`stats.html`の
  騎手別収支タブに追加するか、別画面にするかは要検討

### 実装時の検討事項

- `race_results.horse_name`をキーにした集計のため、表記ゆれ正規化(`horse_notes`と同じ
  空白正規化)を検討する
- 件数が多くなった場合のページネーション・検索条件(期間・競馬場等の絞り込み)の要否
- 個人の予想・購入履歴とは異なり`race_results`は全ユーザー共有データのため、閲覧権限を
  一般ユーザーにも開放するか、管理者限定にするかを着手時に確認する

## 未着手タスク(クラスタ単位)

依存関係と「同じファイル/画面を触るのでまとめて着手すると効率的」という観点でクラスタに
分類したもの。各クラスタ内は優先度(高/中/低)を維持している。クラスタ間の優先順位は
未確定のため、着手前にどのクラスタから進めるかを相談すること。

### クラスタA: 馬券履歴画面(app.js)の小改善

| 優先度 | 内容 | 規模目安 |
|---|---|---|
| ~~中~~ | ~~レースカードの表示順を「日付・競馬場・レース番号(左)」「購入額・払戻額・収支(右)」に変更~~ | **完了(A-1)** |
| 中 | カレンダーの「今日」判定・日付切り替えをJST基準にする | 小〜中 |
| 中 | 複数の履歴を選択してまとめて削除できる機能。一括削除用APIの新設が必要(tickets/ticket-importsでエンドポイントが異なる点に注意。規模が異なるため単独での切り出しを推奨) | 中〜大 |

### クラスタB: 馬券購入画面(buy.js)の改善

出走馬に枠番・馬番が未確定の馬を含むレースは購入不可にするガード(`hasUnconfirmedEntries()`)が
既に実装済みのため、本クラスタの改修時はこのガードと競合しないか確認すること。

| 優先度 | 内容 | 規模目安 |
|---|---|---|
| 中 | レース選択画面(競馬場×R一覧のグリッド)のUIをnetkeiba風に寄せる。①レース番号とレース名の間隔を詰める、②コース種別・距離の情報を2行目に配置し、出走頭数も2行目へ移動する、③競馬場ごとの列幅を狭め、3競馬場開催まではスクロールなしで見切れずに収まるようにする、④出走馬に馬メモ(`horse_notes`)が1件以上登録されている馬が1頭以上出走する場合、頭数表示の右に「✓」マークを表示する(対象はログイン中の自分のメモのみとする想定。他ユーザーのメモは対象外) | 中〜大 |
| 中 | 結果確定済み(`finish_order`または`payouts`が確定済み)のレースを購入した場合、購入と同時にそのレースの確定情報から払戻(`payout`)を即計算して保存する(現状は`payout`が`null`のまま保存され、レース管理画面で払戻を再保存するまで「未確定」表示になる)。`public/payout.js`の`computeTicketPayout`相当のロジックを購入時にも適用する必要があり、`functions/api/tickets/bulk.js`側にレース確定情報を渡す・参照する変更が必要 | 中 |
| ~~低~~ | ~~購入モーダルの初期表示を「単勝が選択され、馬選択画面まで表示された状態」にする~~ | **完了(B-3)** |

### クラスタC: 集計画面(stats.js)

2件目は1件目(レース別4階層化)完了が前提。規模が大きく異なるため同時着手は非推奨。
**クラスタMの成果(騎手名ベースの単勝率等)と重複する可能性があるため、着手時にクラスタMとの
役割分担を確認すること。**

| 優先度 | 内容 | 規模目安 |
|---|---|---|
| 中 | レース別収支を「年月→日付→競馬場→レース」の4階層アコーディオン表示にする | 中〜大 |
| 高 | 競馬場別の収支を、コース別(芝/ダート/障害・距離)の収支に変更。コース情報が無いレースは競馬場全体に集約するフォールバック設計 | 大 |
| 中 | 騎手別収支画面で騎手名をクリックすると、その騎手が関わる購入履歴の詳細一覧を別ウィンドウ(モーダル等)で表示する | 中 |
| 中 | レース別収支画面でレース名をクリックすると、そのレースの購入履歴の詳細一覧を別ウィンドウ(モーダル等)で表示する | 中 |

### クラスタD: 管理画面・権限表示(admin.js/admin.html)

| 優先度 | 内容 | 規模目安 |
|---|---|---|
| 中 | 「未登録レース一覧」からレース情報登録まで画面遷移せず完結できるようにする(モーダル共通化 or admin.html側に個別実装。設計方針は着手時に要相談) | 大 |

### クラスタE: CSVインポート仕様変更

| 優先度 | 内容 | 規模目安 |
|---|---|---|
| 中 | 取り込んだ結果は外れ馬券も含めすべて「確定」扱いにする(`hitText===''`の場合もpayout=0にする) | 小〜中 |
| 中 | 返還(出走取消等)行の`payout`計算を、実際のCSV表記を確認したうえで見直す(上記「調査中の不具合」表参照)。サンプルCSV入手が前提 | 小〜中 |

### クラスタF: 馬券かご機能(独立した大型機能)

仕様確定済み・単独で着手可能。詳細は下記「馬券かご 確定仕様」参照。

| 優先度 | 内容 | 規模目安 |
|---|---|---|
| 高 | netkeiba・楽天競馬のような「馬券かご」機能(複数の買い目をかごに入れてまとめて購入・保存) | 大 |

### クラスタG: レース結果一括登録の残課題

外部サイトへの自動アクセスは行わない方式(ユーザーが手動保存したPDFの解析)のため、
bot対策等の技術的制約は無く着手可能。「JRAレース結果PDFをインポート」機能自体は実装済み
(仕様は`docs/DESIGN.md`「JRAレース結果PDFインポート」参照)。残課題は上記「調査中の不具合」
表の払戻マージ・矢印区切り対応、および実機検証。**クラスタLの実装が完了次第、本クラスタの
残課題(fill-emptyのみ問題)に着手することを検討する。**

### クラスタH: 外部データ自動取得(保留)

JV-Link(Windows専用ActiveX)・JRA公式サイト(bot対策あり)・netkeiba(規約違反)のいずれも
技術的/規約的な制約があり保留中。詳細は下記「出走情報・払戻情報の自動取得について(調査結果)」
参照。方針転換する場合のみ再検討する。

| 優先度 | 内容 | 規模目安 |
|---|---|---|
| 中(保留) | レース登録画面に「出走情報取得」ボタン(枠番・馬番・馬名・騎手名・種牡馬名を自動反映) | 大 |
| 中(保留) | レース登録画面に「払戻情報取得」ボタン(着順・払戻を自動反映) | 大 |

### クラスタI: リファクタリング候補

| 候補 | 内容 | リスク |
|---|---|---|
| `formatDate`の重複整理 | `formatDate`が`admin.js`・`prediction.js`・`races.js`・`stats.js`・`utils.js`の5箇所に存在し、出力形式がそれぞれ異なる(年あり/なし、曜日あり/なし等)。`utils.js`にオプション引数を持たせるか、用途別の複数関数を用意して集約し、各画面は現状と同じ見た目になるよう対応する関数を呼び出す形に変更する。着手前に各画面の表示例を記録しておくと比較しやすい | 中(表示形式の回帰に注意。自動テストが無いため目視確認が必要) |
| `buy.js`(700行超)・`races.js`(700行超)の巨大ファイル分割 | 状態管理・API呼び出し・DOM描画・イベント登録が1ファイルに混在している。本アプリの中核機能であり自動テストが無いため、壊れた場合の影響が大きい。一度に全体を作り直すのではなく、関心事ごとに段階的に切り出す。着手前に分割方針をユーザーに提示して承認を得ること | 高 |
| CSSキャッシュバスティングの一元化 | `style.css`変更のたびに全HTMLファイルの`?v=YYYYMMDDHHmm`クエリパラメータを手作業で書き換える運用(`docs/DESIGN.md`「CSSのキャッシュ対策」参照)。根本対応にはビルドツール導入が必要だが、本プロジェクトは「Node不要・wranglerのみで手動運用」という方針を掲げており、ビルド前提にするのは方針と衝突する。**着手しない方針を推奨**(将来的に再検討) | - |
| トラック(競馬場)リストの重複整理 | `JRA_TRACKS`(`parse.js`)・`JRA_ENTRIES_TRACKS`(`jra-entries-pdf.js`)・`JRA_RESULT_PDF_TRACKS`(`jra-result-pdf.js`)が同一内容の配列として3箇所に重複している。`utils.js`等への集約を検討 | 低(内容が完全一致のため単純な置き換えで済む見込み) |

### クラスタJ: 予想登録画面(prediction.js)の追加改善

「このレースの購入馬券」欄の金額編集機能と、モバイル表示のレイアウト崩れ修正をまとめたクラスタ。
後半4件はいずれも同じ画面(`prediction.html`/`prediction.js`)のモバイル(`430px`以下)表示に
関するCSS修正のため、まとめて着手すると効率的。

| 優先度 | 内容 | 規模目安 |
|---|---|---|
| ~~中~~ | ~~「このレースの購入馬券」欄の各買い目行で、購入金額をその場で編集できるようにする(`index.html`購入履歴画面の`.amount-edit-input`と同様のinput+`PUT /api/tickets/:id`呼び出しを追加する想定)~~ | **完了(J-1)** |
| ~~低~~ | ~~モバイルUI: 予想登録画面上部の説明文(「予想印は馬名の横で変更できます。馬をクリックすると、この馬についてのメモだけを入力できます。印とメモは変更時に自動保存されます。」)をモバイル(`600px`以下等)では非表示にする~~ | **完了(J-2)** |
| ~~中~~ | ~~モバイルUI: 馬行(`.horse-note-toggle`)で、騎手名と予想印セレクト(`.prediction-mark-select`)の間の余白が広く、セレクトが画面端で見切れてしまう不具合を修正する(添付スクリーンショット参照。グリッド列幅・要素幅の見直しが必要)~~ | **完了(J-3)** |
| ~~中~~ | ~~モバイルUI: 馬行のメモ展開トグル「＋」が改行されて次の行に落ちてしまう不具合を修正する。あわせて、メモが登録済みの馬の行は背景色を馬券購入画面の「購入済み」行と同じ配色(`.race-column-row.purchased`で使っている win系の薄い緑)に変更する~~ | **完了(J-4)** |
| ~~低~~ | ~~モバイルUI: メモ入力欄のプレースホルダー・説明文「次回以降も参照したい、この馬固有のメモを入力」を削除する~~ | **完了(J-5)** |

### クラスタK: モーダル/ダイアログの共通UI改善

| 優先度 | 内容 | 規模目安 |
|---|---|---|
| 中 | 出走馬編集(`race-entries-modal`)・払戻編集(`race-payout-modal`)・出走馬一覧PDFインポート・JRAレース結果PDFインポート・開催日程一括登録(`schedule-modal`。以上いずれも`races.html`)、および馬券購入モーダル(`purchase-modal`。`buy.html`)など、ダイアログ表示系すべてで、ESCキー押下時に「キャンセル」ボタンと同じ扱いで(保存せず)閉じるようにする。共通のキーハンドラ(`keydown`で`Escape`検知→各モーダルのcancel/close関数を呼ぶ)を用意し、対象モーダルへ横断的に適用する想定 | 小〜中 |

(仕様矛盾の解消方針一覧は`docs/BACKLOG.md`「参考: 今回確認した仕様矛盾の解消方針」参照)

## 出走情報・払戻情報の自動取得について(調査結果)

「レース登録画面/払戻入力画面に取得ボタンを置き、開いているレース1件分だけをその場で
取得する」というUI案が出たが、1件ずつであっても呼び出せるHTTP経由の取得口がそもそも
存在しないため、保留とする。

- **JRA-VAN Data Lab.**(公式・月額サブスクリプション): 唯一のインターフェースが
  「JV-Link」というWindows専用のActiveX COMコンポーネント。Cloudflare Pages Functions
  (サーバーレスのJavaScript実行環境)からは技術的に呼び出せない
- **JRA公式サイト(jra.go.jp)への直接アクセス**: bot対策により即座にブロックされることを
  確認済み。利用規約の文言以前に、技術的にアクセスできない
- **netkeiba**: 利用規約でスクレイピングを明示的に禁止している

選択肢(いずれも保留中。方針転換する場合は下記から選ぶ):

- A. 貼り付け方式・PDFアップロード方式を磨く方向で進める(現状の方針。出走馬一覧PDF
  インポート・JRAレース結果PDFインポートもこの延長で対応している)
- B. 有料のスクレイピング代行API(Bright Data等)を利用する。技術的には可能だが月額コストが
  発生し、対象サイトの規約違反リスクが完全になくなるわけではない
- C. Windows常時稼働機を別途用意し、JV-Linkを使った中継サーバーを立てる(JRA-VAN公式のため
  規約面は最もクリーンだが、インフラ運用の手間が大きい)

## 馬券かご 確定仕様

目的: 複数レース・複数式別にまたがる買い目を、都度保存せずに一時的にためておき、
最後にまとめて保存する。netkeiba・楽天競馬の「馬券かご」相当。

確定した仕様:

- 対象範囲は「レースをまたいでよい」。複数レース・複数式別の買い目を1つのかごに貯められる
- 現行の「式別を選んで組み合わせ選択→即保存」という都度保存の購入フローは廃止し、
  「かごに追加」→(必要なだけ繰り返す)→「まとめて購入する」の流れに一本化する
  (1点だけ買いたい場合も、必ずかごを経由する)
- かごの中身はブラウザ/端末を閉じたり、後日開き直しても保持されている必要がある。
  → サーバー保存(下書きテーブル)までは不要と判断し、localStorageに保存する方式とする
    (同一端末・同一ブラウザ内でのみ保持され、他の端末とは同期されない想定。複数端末での
    同期が必要になった場合は、サーバー側に下書き用テーブルを追加する改修が別途必要になる)
- 「まとめて購入する」実行時は、かご内の全買い目を1回のAPIリクエストで送信し、サーバー側は
  `db.batch()`でまとめて書き込む(行ごとに個別リクエストをループする実装にはしない。
  過去にCSVインポートでN+1クエリによるサブリクエスト数上限超過が起きた教訓を踏まえる)

画面イメージ:

- 購入画面(buy.html)に「かご」パネルを常設(右側 or 下部)。ヘッダー等にも
  「かご: n点 / 合計¥xxx」のように現在の内容を常時表示する
- かご内の各行: 日付・競馬場・レース番号・式別・買い目内容・金額・削除ボタン
- かご下部: 合計点数・合計金額、「まとめて購入する」ボタン、「かごを空にする」ボタン
- 保存成功後はかごを空にし(localStorageもクリア)、購入履歴に反映する

実装時の主な変更点(見積り):

- buy.js: 「購入する」ボタンを「かごに追加」ボタンに置き換え、かご状態(localStorage)の
  読み書き・かごパネルの描画・合計計算を追加。レース遷移時もかごの内容を保持する
- functions/api/tickets/bulk.js(または同等の一括保存API): 複数レース・複数式別が混在した
  買い目配列を受け取れるよう、リクエスト形式を拡張(現状は1レース分の想定になっていないか要確認)
- localStorageのキーはログインユーザーごとに分ける(同じ端末を家族で共用した際にかごが
  混ざらないようにする)配慮が必要
