# 未着手タスク・引継ぎメモ

このファイルは、要望としては承認済みだが未実装のタスクを引き継ぐための一覧です。
`README.md` / `docs/DESIGN.md` / `docs/TESTING.md` とは異なり「現状」ではなく
「これから対応すること」を記録する場所です。着手したら該当タスクをここから削除し、
`docs/DESIGN.md`(必要なら`docs/TESTING.md`も)に現状として反映してください。

## 🔰 次のチャットで最初に読むこと(引き継ぎサマリー)

- アプリの全体像は`README.md`、データ構造・仕様の詳細は`docs/DESIGN.md`、
  手動テスト項目は`docs/TESTING.md`を参照(いずれも現状を反映した生きたドキュメント)
- 作業の進め方は毎回同じパターン: **①要望の仕様矛盾・不足を確認→承認を得る
  →②`docs/DESIGN.md`等を仕様に合わせて更新→③実装→④`node --check`で構文チェック
  →⑤`docs/BACKLOG.md`のタスク表・完了履歴を更新→⑥zipに固めてダウンロード提供**
- 実機(wrangler dev / 本番)での動作確認は毎回**未実施**。コードレビューと
  Node上でのロジック単体テスト・シミュレーションのみで検証している。
  実機確認はユーザー側で行ってもらっている状況
- 2026-07-31に回答待ちだった確認事項2件は解消済み:
  1. 通常購入(tickets)のロック仕様 → **全面撤廃で承認・実装済み**(フェーズ11参照)。
     本アプリは通常の馬券購入アプリというより履歴管理がメインという位置づけのため、
     厳格なロックは不要という判断
  2. 騎手別収支が表示されない件 → ユーザーより「今のところ順調に見える」と確認済み。
     フェーズ9のバックフィル機能で解消したとみられる(引き続き気になる挙動があれば再調査)
- 2026-08-01: CSV取込履歴が全く表示されなくなる障害が発生・修正済み(フェーズ12参照)。
  原因はCloudflare Workersのサブリクエスト数上限超過(N+1クエリ)。同種の実装パターン
  (ループ内で1件ずつD1へ問い合わせる)が他のAPIにも残っていないか、今後新規実装時は注意する
- 2026-08-01: **複数ユーザー対応を実装済み**(フェーズ13参照)。個別ログイン(自己登録制)+
  データのユーザー単位分離+レース管理の管理者限定化を実装した。
  **本番DBへ適用する際は、`latest1.sql`(v13セクション)を実行→管理者アカウントを
  作成(ログイン画面の「新規ユーザー登録」)→`ADMIN_USERNAMES`環境変数にそのユーザー名を
  追加→`assign_existing_data_to_admin.sql`を実行、という順番が必要**(README参照)。
  初期シードとして`username=admin`/`password=password`のアカウントが自動作成される
- 完了済みタスクの詳細な経緯は下記「2026-07-30時点の状況」以降のフェーズ1〜13に
  記録してある(新しいセッションで同じ修正を重複して行わないための参考)

## 2026-07-30 時点の状況

直近で以下を完了しました(詳細は`docs/DESIGN.md`参照)。

**バグ修正(フェーズ1)**
- CSV取込: 複数的中組み合わせの払戻金額計算、重複判定キーへの日付追加
- レース新規登録時に着順・払戻が保存されない不具合の修正

**フェーズ2(小規模改善)**
- 予想登録画面: 騎手名を馬名の後ろ(同じ行)に表示
- 予想登録画面: 予想印をドロップダウン化し、1頭1印に制限(承認済み仕様)
- レース管理画面: 払戻入力画面の「0点購入」表示を非表示に
- レース管理画面: 出走頭数を5〜18頭の範囲に変更、頭数選択時に枠番の初期値を自動設定

**フェーズ3(小規模改善・追加分)**
- 予想印の選択肢に「消」を追加(◎○▲△☆消の6種類)
- 予想印の「−(印なし)」表記を「−」のみに簡略化
- レース管理画面のヘッダーナビは他画面と同じ4タブに統一(誤って「馬券購入」のみに
  していたのを差し戻し)。代わりに、レース一覧の各行(アコーディオン内)にあった
  「馬券購入」ボタンを削除(一般ユーザー画面への遷移は、レース一覧行からではなく
  ヘッダーナビの「馬券購入」タブからのみ行う、という意図だったため)
- 「馬券帳」タイトルのフォント統一: `buy.html`にWebフォント(Shippori Mincho等)の
  読み込みタグが無く、そのページだけデフォルト書体になっていたのを修正

**フェーズ4(優先度「高」2件)**
- レース編集画面: 出走馬表とは独立した「1着/2着/3着」馬番セレクトを追加し、
  馬名・出走馬表が未登録でも払戻金額を入力できるように変更(`finish_order`は上位3着のみ保存)
- レース確定後(着順 or 払戻確定)は、そのレースへの新規馬券購入をAPI・フロント両方で禁止

**フェーズ5(カレンダー関連2件)**
- 購入履歴(index.html)画面: カレンダーを常時表示に変更し、「日付から選ぶ」ボタンを削除。
  初期表示はサマリー(総購入・総払戻・収支・回収率)とカレンダーのみとし、
  購入履歴の詳細はカレンダーで選択した日付分だけ表示
- レース管理画面にもカレンダーを追加し、登録済みレースがある日をマーク、
  日付クリックでその日のレースだけに絞り込めるように変更

**フェーズ6(集計画面の列ソート)**
- レース別・競馬場別・騎手別の3表とも、列見出し(購入/払戻/収支/回収率/的中率、
  および先頭列)クリックで昇順⇔降順を切り替えられるように変更
- レース別収支の初期表示を日付・競馬場・レース番号の順にソート

**フェーズ7(バグ修正2件+仕様転換1件)**
- 集計画面のソート機能で「購入」「払戻(確定)」列がソートできないバグを修正
  (`STAT_COLUMNS`のキー名が`computeGroupStats`の実際のプロパティ名と食い違っていた)
- 集計の各画面から「購入枚数(未確定)」の表示を削除
- レース確定後の購入禁止(フェーズ4で実装)を撤回。購入履歴の登録し忘れに対応できるよう、
  確定後も新規購入は引き続き可能に変更。予想画面のボタンは「結果確定済(購入する)」表示に、
  購入画面は案内バナー表示のみ(ブロックしない)に変更

**フェーズ8(2頭軸UIのJRA/netkeiba化)**
- 三連複・三連単の「2頭軸」購入画面を、1つの馬番リストに「軸」(最大2頭)・「相手」の
  チェック列を並べる形式に変更(以前は「軸馬①/軸馬②/相手」の3つの縦リストが並んでいた)
- 軸と相手は排他(軸に選んだ馬は相手に選べない、その逆も同様)。未使用になった
  3列レイアウト用CSSも削除

**フェーズ9(CSVインポート関連2件)**
- CSV取込結果のうち、着順が一意に定まる情報(単勝1着/馬単1・2着/三連単1〜3着)を
  `races.finish_order`へ、的中組み合わせごとの払戻レートを式別ごとに`races.payouts`へ反映
  (シミュレーションで実データ相当のCSVを使い動作確認済み)
- レース管理画面で出走馬表(馬名・騎手)を登録・更新すると、同じレース・馬番を参照している
  CSV取込データ・購入履歴(`imported_ticket_items`/`tickets`)へ馬名・騎手を書き戻す
  (バックフィル)処理を追加。共有ヘルパー`functions/api/_shared.js`の
  `backfillHorseNamesForRace`として実装し、レース新規登録・編集の両方から呼び出す

**フェーズ10(予想登録画面のレイアウト再設計)**
- ヘッダーの表示順を「日付・競馬場名・レース番号・レース名・頭数」に変更
- 競馬場名・レース番号をセレクト化し、ページ遷移なしで同じ開催日の別レースへ切り替え可能に
  (カレンダーへ戻る手段がない問題への対応)
- 「このレースの馬券を購入」ボタンをヘッダー内(レース名の近く)に配置し、
  スクロールなしで購入画面へ進めるように変更
- 馬一覧を上部、購入済み馬券一覧を下部に配置する順序に変更

**フェーズ11(ロック仕様の全面撤廃・騎手別収支の状況確認)**
- 通常購入(`tickets`)のロック仕様(着順・払戻確定後の編集・削除禁止)を全面撤廃。
  `functions/api/tickets/[id].js`の`PUT`/`DELETE`から409を返すロックチェックを削除し、
  確定後でも購入額・払戻・メモの編集、削除ができるように変更。
  `docs/DESIGN.md`「ロック仕様」の節、`docs/TESTING.md`の該当項目も更新
- 騎手別収支が表示されない件は、ユーザー確認により現状問題なし(フェーズ9のバックフィル
  機能で解消したとみられる)。要調査事項としてはクローズ

**フェーズ12(CSV取込履歴が表示されなくなる障害の修正)**
- 症状: CSVインポート中に「Too many API requests by single Worker invocation」
  エラーが発生し、以降`GET /api/ticket-imports`が500エラーとなってインポート履歴が
  画面に一切表示されなくなった
- 原因: `functions/api/ticket-imports/index.js`が典型的なN+1クエリになっていた
  - `GET`: `imported_ticket_groups`を取得後、**グループごとに**`imported_ticket_items`を
    個別クエリしており、取込件数(グループ数)が増えるほどD1への問い合わせ回数が増加。
    Cloudflare Workersの1リクエストあたりのサブリクエスト数上限に達し500エラーになっていた
  - `POST`(CSV取込本体): CSVの**行ごとに**「既存重複チェック」「レース参照(SELECT/INSERT)」
    「重複候補検出」を個別クエリしており、行数の多いCSVを取り込むと同じ上限に抵触していた
    (このPOST側の失敗が、添付画像1枚目のエラーの直接の発生源とみられる)
- 対応:
  - `GET`: グループを取得後、`imported_ticket_items`は全件を1回のクエリでまとめて取得し、
    メモリ上でグループごとに振り分ける方式に変更(クエリ回数がグループ数に依存しない定数回に)
  - `POST`: 既存重複チェック用のキーをリクエスト開始時に1回だけ取得してメモリ上で照合、
    レース参照は行内でキャッシュして同一レースへの再問い合わせを回避、重複候補検出は
    全行の取込完了後に1回のクエリでまとめて行うよう変更(1行あたりのクエリ回数を削減)
  - Node.js標準の`node:sqlite`でD1互換のモック(`prepare/bind/first/all/run/batch`)を作り、
    実際にCSV取込→一覧取得→同一ファイル再取込時の重複スキップ→着順/払戻のレース反映、
    までを動作確認済み(30行/6レースのCSVで、`GET`のクエリ回数はグループ数によらず3回、
    `POST`のクエリ回数も従来実装比で大幅に削減されることを確認)
- 既知の限界: CSVの行数が非常に多い場合(数百行規模)は、行ごとに発生する書き込み系クエリ
  (取込元行・グループ・レース更新など)自体は削減しきれておらず、依然として上限に達する
  可能性が残る。再発する場合はCSVを分割して取り込む、または書き込みも`db.batch()`で
  まとめて1リクエスト化するさらなる改修を検討する

**フェーズ13(複数ユーザー対応の実装・2026-08-01)**
- `users`テーブルを新設し、ログイン画面から自己登録(ユーザー名+パスワード、招待コード無し)
  できるようにした。管理者判定は環境変数`ADMIN_USERNAMES`で行い、DBには管理者フラグを
  持たせない設計にした
- `tickets`・`imported_tickets`・`imported_ticket_groups`・`prediction_notes`・
  `prediction_marks`・`horse_notes`に`user_id`を追加し、全APIエンドポイントを
  ユーザー単位のフィルタ・所有者チェックに対応させた(他ユーザーのデータは404扱い)
- レース情報(`races`)の登録・編集・削除・開催日程一括登録を管理者限定にした
  (`functions/api/_shared.js`の`requireAdmin()`でガード)
- CSVインポート時のレース自動作成を廃止し、未登録のまま取り込まれたデータは
  `race_id IS NULL`のまま保持。管理者が対応するレースを登録すると
  `linkUnregisteredImportsToRace()`により自動的に紐付く仕組みを実装した
- 管理者向け画面を新設: `admin.html`(「未登録レース一覧」「登録ユーザー一覧」)
- 初期管理者アカウント(`username=admin`, `password=password`)を`schema.sql`・
  `latest1.sql`双方にシード。パスワードハッシュ(PBKDF2)はNode.jsの`crypto`と
  Cloudflare Workers上の`crypto.subtle`(WebCrypto)で相互に検証可能であることを
  実際にNode上でテストして確認済み
- 既存本番DBへの適用手順を`README.md`に追記(`latest1.sql`のv13適用→管理者アカウント
  作成→`assign_existing_data_to_admin.sql`実行、の順)
- `node:sqlite`によるD1互換モックで、ユーザー登録・ログイン・購入履歴のユーザー分離・
  管理者権限チェック・CSV取込→未登録レース一覧→レース登録での自動紐付け、まで
  一通り動作確認済み(自動テストスクリプトで全項目パス)

**フェーズ14(モバイル対応・2026-08-02)**
- 購入画面(`buy.html`)の組み合わせ選択表(`.bet-horse-row`。単勝〜三連単・2頭軸まで共通の
  馬選択UI)について、既存の`700px`ブレークポイントに加え、`430px`以下(iPhone SE3等の
  小型スマホ)向けの追加メディアクエリを実装。馬名は1行固定+はみ出し省略、チェックボックスは
  横並び維持のまま間隔・フォントサイズを縮小、枠番・馬番列もさらに縮小。`430px`超(PC含む)の
  レイアウトは変更なし
- 実装後の実機確認で「馬名が1文字しか見えない」不具合が判明。ヘッドレスブラウザ(Playwright)で
  実際のグリッド配置を検証し、印(`.bet-horse-mark`)列に残っていた旧ルールの`grid-row:1`が
  原因と特定(印列が列1に割り込み、枠番・馬番・馬名が1列ずつ右にズレていた)。
  `.bet-horse-mark`に`grid-row:auto`を明示して解消。修正後は馬名がフル表示されることを
  ヘッドレスブラウザのスクリーンショットで確認済み
- 購入履歴画面(`index.html`)のCSVインポートボタン(`#csv-import-btn`)を、既存の`700px`
  ブレークポイントで非表示化。CSVインポート機能・APIは維持し、表示のみ制御
- `docs/DESIGN.md`に「レスポンシブ対応」の節を新設して反映

残りの要望は未着手です。優先度・規模の見積もりとあわせて以下に記録します。

## 未着手タスク一覧(優先度・規模つき)

| 優先度 | 内容 | 規模目安 | 備考 |
|---|---|---|---|
| 中 | レース管理画面: 日付選択後の一覧で「未登録」の行をクリックすると、該当する競馬場・レース番号があらかじめ入力された状態でレース登録画面(モーダル)が開くようにする | 小〜中 | 現状、`races.js`の「未登録」行(`.no-race`)にはクリックイベントが未設定。`openModal()`が日付・競馬場・レース番号を事前入力できるよう拡張すれば対応可能 |
| 中 | 馬券履歴画面: 日付をクリックしたときに表示される履歴を競馬場・レース番号の昇順でソートする | 小 | `app.js`の`groupByRace`が現状`race_date`のみで降順ソートしているため、同日内の並び順(track/race_number)が未定義。ソートキーに`track`,`race_number`を追加すれば対応可能 |
| 中 | 馬券履歴画面: レースカードの表示順を「日付・競馬場・レース番号(左)」「購入額・払戻額・収支(右)」の順に変更する | 小〜中 | `app.js`の`renderRaceCard`のヘッダーHTML構成を組み替える。現状は矢印→レース番号→競馬場→レース名→日付→金額群の順 |
| 低 | 馬券履歴ページ・集計ページの初期表示の高速化 | 不明 | **確認済み(2026-07-30)**: 体感は「数秒・我慢できる範囲」とのことで緊急性は低い。着手する場合は`GET /api/tickets`等のレスポンスタイム・件数を計測してから対応要否を判断する |
| 中 | レース登録画面に「出走情報取得」ボタンを設け、開催日・競馬場・レース番号を入力して「データ取得」を押すと、出走馬の枠番・馬番・馬名・騎手名・種牡馬名を取得してテキストボックスへ反映する。種牡馬名は現状扱っていないデータのため、新規に取り扱えるようにする | 大 | **保留(2026-08-01調査済み)**。詳細は本ファイル末尾「出走情報・払戻情報の自動取得について(調査結果)」を参照。JV-Link(JRA-VAN公式)はWindows専用ActiveXでサーバーレス環境から呼べず、JRA公式サイトは実際にアクセスするとbot対策でブロックされることを確認済み、netkeibaは規約でスクレイピング禁止。**当面は着手しない**方針で合意済み。方針転換する場合は前述ファイル内の選択肢(B: 有料スクレイピング代行API / C: Windows常時稼働機でのJV-Link中継)を参照 |
| 中 | レース登録画面に「払戻情報取得」ボタンを設け、開催日・競馬場・レース番号の入力があれば着順・払戻の情報を取得してテキストボックスへ反映する | 大 | 上記と同じ理由で**保留**。方針は上記参照 |
| 低 | レース編集画面: 「テキストから出走馬を一括入力」ボタンの横にも「保存する」ボタンを配置する | 小 | 現状、保存ボタンはモーダル下部の1箇所のみ。出走馬を貼り付け入力した直後にすぐ保存できるよう、貼り付けボタンの近くにも保存ボタンを複製する |
| 中 | レース管理画面: 日付選択後の一覧のボタンを「レース情報登録」「払戻登録」の2つに分割する(入力タイミングが異なるため)。ボタンの文言で登録済み/未登録が判別できるようにする | 中 | 現状は1レース1ボタン(編集モーダルを開くのみ)。出走馬表(entries)と払戻(finish_order/payouts)を別々に開始できるモーダル(または画面遷移)に分割する必要がある。ボタンラベル例:「出走馬表 登録済/未登録」「払戻 登録済/未登録」 |
| 中 | レース編集画面: 払戻入力部分の幅が広すぎてスクロールが多い。もう少しコンパクトなレイアウトにする | 小〜中 | `races.js`の`renderPayoutBlocks`・関連CSSの調整。フェーズ4で1〜3着セレクト化はしたが、払戻レート入力欄自体のレイアウトは未着手 |
| 高 | レース情報に「芝・ダート・障害」の種別と「距離」を持たせる | 中 | DBスキーマ変更が必要(`races`テーブルへのカラム追加、`schema.sql`・`latest1.sql`・`docs/DESIGN.md`の更新)。レース登録・編集画面のUIにも入力欄を追加する |
| 中 | 馬券購入画面の一覧の1行の情報を「レース番号・レース名・芝/ダート/障害・距離・頭数」にする | 中 | 上記の芝・ダート・障害/距離のデータ追加が前提。`buy.js`のレース一覧行のレンダリングを変更 |
| 中 | 馬券購入画面で、カレンダーから日付を選択した後に表示されるレース一覧について、馬券購入済みのレースであることが見た目で分かるようにする(未購入のレースがどこかを判別したい) | 中 | `buy.js`のレース一覧描画時に、そのレースのチケット有無を判定してバッジ等で表示する。`GET /api/tickets`との突合が必要 |
| 中 | CSVインポートで取り込んだ結果は、外れ馬券も含めすべて「確定」として扱う(未確定分は無くす)。的中していない買い目の払戻は「0」とする | 小〜中 | `functions/api/ticket-imports/index.js`の`onRequestPost`で、現状は「的中／返還」列が空文字の行だと`payout=null`(未確定)のままになる。すべて確定扱いにするには、`hitText===''`の場合も既定で`payout=0`とするよう分岐を見直す(的中判定`isHit`のロジック自体は変更不要) |
| 中 | レース別収支画面をアコーディオンで「年月→日付→その日のレース」の3階層表示にする(レース数が増えると縦に長くなるため) | 中〜大 | `stats.js`のレース別集計テーブルの表示構造の見直しが必要。現状は日付降順のフラットな一覧とみられるため、年月・日付でグルーピングし開閉可能なUIへ変更する |
| 高 | netkeiba・楽天競馬のような「馬券かご」機能を追加したい(複数の買い目をかごに入れてからまとめて購入・保存する) | 大 | **仕様確定(2026-08-01)・実装待ち**。詳細は本ファイル末尾「馬券かご 確定仕様」を参照 |
| 低 | 馬券購入画面(モーダル)を開いたときの初期表示を「単勝が選択され、馬選択画面まで表示された状態」にする | 小 | `buy.js`の`resetState()`で`state.betType`は既に`"tan"`になっており、ボタンの選択表示(`renderBetTypes`)も単勝がハイライトされる。ただし`openPurchase()`内では`renderBetTypes()`止まりで、単勝クリック時のみ実行される馬選択画面表示(`renderPicker()`)が初期表示では呼ばれていない。初期表示時にも単勝相当の処理を呼べば対応可能 |
| 低 | 購入履歴画面で、履歴を削除した直後にその履歴の表示がすぐ消えるようにする | 小 | `app.js`の削除処理は`alert("購入を削除しました。")`の後に`loadTickets()`(再取得・再描画)を呼んでいる。`alert()`はブラウザ標準のモーダルで処理をブロックするため、ユーザーがOKを押すまで一覧が更新されないように見えている可能性が高い。`alert()`を廃止(または非ブロッキングな通知に変更)し、削除成功後すぐに`loadTickets()`が反映されるようにすれば解消できると見込まれる |
| 中 | 購入履歴画面に、複数の履歴を選択してまとめて削除できる機能を追加する | 中〜大 | チェックボックスによる複数選択UIと、選択済みIDをまとめて削除する処理が必要。通常購入(`tickets`)とCSV取込分(`ticket-imports`)でAPIエンドポイントが異なる点に注意(一括削除用のエンドポイントを新設するか、選択件数分`DELETE`をループ実行するかは要検討。後者は今回の障害の教訓を踏まえ、可能なら一括削除用のAPIを1本用意する方が安全) |
| 低 | 購入履歴カレンダーの日別収支サマリを「K」表記(千円単位の概算)ではなく、実際の数値で表示する | 小 | `app.js`の`renderHistoryCalendar`内、`Math.round(profit / 1000)}k`の箇所を実数表示に変更すればよい。桁数が増えるためカレンダーマス目のレイアウト(文字サイズ・幅)の調整が必要になる可能性がある |
| 高 | ~~今後ユーザー追加機能(複数ユーザー対応)を見据え、馬券購入履歴・インポート履歴をユーザー単位で管理できるようにする~~ **実装済み(2026-08-01)** | 大 | フェーズ13参照。本番DBには`latest1.sql`のv13適用+`assign_existing_data_to_admin.sql`の実行が必要(README参照) |
| 中 | レース管理画面で、過去に登録した全レースを表示すると縦に長くなるため、カレンダーで表示中の月の分だけ表示されるようにする | 中 | `races.js`のレース一覧取得・描画箇所を、現在の対象月(カレンダーの表示月)でフィルタするよう変更する。`GET /api/races`側で日付範囲を絞れるようにするか、フロント側で受け取った全件を月でフィルタするかは実装コストを見て判断 |
| 中 | 予想登録画面で、各馬のアコーディオンを開かなくても、その馬にメモが登録されているかどうかが分かるようにする | 小 | `public/prediction.js`の`renderHorses()`には実は既に`.horse-note-preview`という小さいプレビュー文字(メモの先頭36文字)を馬名の下に表示する仕組みがあるが、目立たず気づきにくい状態になっている。改善案: 馬名行に「メモあり」を示すアイコン・バッジ(📝等)を追加する、プレビュー文字のフォントサイズ・色を強める、などレイアウト面の改良で対応できそうだが、要デザイン確認 |
| 低 | パスワードを忘れた場合の救済措置(パスワードリセット等)を用意する | 中 | 複数ユーザー対応(自己登録制)の導入後に必要性が出てくる想定。今回のスコープには含めない。メール等の連絡手段を持たないアプリのため、実装方式(秘密の質問・管理者による手動リセット等)は着手時に要検討 |
| 高 | 現在は競馬場別に出している収支を、コース別(芝/ダート/障害・距離等)の収支に変更したい。まずはレース管理画面でコース情報を入力できるようになってからでよい。コース情報が無いレースは一旦「競馬場全体」として集約しておき、後日コース情報が入力された時点でコース別に再集計されるようにしたい | 大 | 前提として「芝・ダート・障害+距離」のスキーマ拡張タスク(本表に既出、優先度高)が必要。その上で`stats.js`の集計軸(現状は`track`単位)にコース単位のグルーピングを追加する。コース未入力のレースは`track`単位のフォールバック集計にする設計(要件通り)。実装順序としては、レース管理画面へのコース入力欄追加→スキーマ拡張タスクの完了後に着手するのが自然 |
| 低(将来) | 集計とは別に、データ分析機能を用意したい。①騎手情報: 騎手を選択すると各コース別の成績を表示 ②枠特性: コース別に枠順ごとの単勝率・複勝率を表示 ③コース別得意騎手: コース別の好成績騎手トップ10 ④コース別得意種牡馬: コース別の好成績種牡馬トップ10 | 大 | 優先度低・将来的にやれたら嬉しい程度、とのこと。上記「コース別収支」(コース情報の整備)が前提。さらに④は種牡馬データの整備(現状未対応。「出走情報取得」タスクの種牡馬名反映が前提になりうる)も必要。母数(自分の購入・インポートしたデータ)だけでは統計として心もとない可能性がある点も、着手時に方針を相談したい(全レースの結果データを一定範囲収集する必要があるかもしれない) |
| 中 | ~~iPhone SE3等の小さい画面で馬券購入画面(組み合わせ選択表)を開くと、チェックボックスが縦に並んでしまう、枠番・馬番列の幅が広すぎる、馬名列が狭くて馬名が複数行に折り返ってしまう、といったレイアウト崩れがある~~ **実装済み(2026-08-02・フェーズ14)** | 中 | `430px`以下の追加ブレークポイントで対応。詳細は`docs/DESIGN.md`「レスポンシブ対応」参照 |
| 低 | ~~iPhone SE3等のスマホで馬券履歴画面を開いたとき、CSVインポート機能(ボタン等)を非表示にしたい~~ **実装済み(2026-08-02・フェーズ14)** | 小 | 既存の`700px`ブレークポイントを流用して`#csv-import-btn`を非表示化。機能・APIは維持 |

## 開発効率化のためのタスク分類(2026-08-02)

上の「未着手タスク一覧」を、優先度(高中低)だけでなく**依存関係**と
**着手のまとまりやすさ**の観点でカテゴリ分けしたもの。カテゴリ単位で
まとめて着手すると、関連ファイルの手戻りが少なく効率的に進められる。
新規タスクを追加する際も、該当カテゴリに追記すること。

### 🅰 スキーマ拡張系(依存関係あり・上から順に着手)
1. レース情報に「芝/ダート/障害」区分+距離を追加(優先度:高 / 規模:中)
2. コース別収支への変更(優先度:高 / 規模:大、①に依存)
3. 購入画面の一覧にコース/距離/頭数を表示(優先度:中 / 規模:中、①に依存)
4. データ分析機能・将来対応(優先度:低(将来) / 規模:大、①②に依存)

### 🅱 馬券かご機能(独立した大型機能)
5. 馬券かご(優先度:高 / 規模:大、仕様確定済み・単独で着手可能)

### 🅲 購入・履歴画面のUI改善(小〜中規模・個別に着手しやすい)
6. 馬券履歴: 同日内の履歴を競馬場・レース番号順にソート
7. 馬券履歴: レースカードのレイアウト順変更
8. 購入履歴: 削除直後に一覧から即座に消えるようにする
9. 購入履歴カレンダー: 日別収支を実数表示
10. 購入履歴: 複数選択でまとめて削除
11. 馬券購入画面: 購入済みレースのバッジ表示
12. 馬券購入モーダル: 初期表示の改善(単勝選択済み状態)

### 🅳 レース管理画面のUI改善(まとめて着手しやすい)
13. 未登録行クリックで日付・競馬場・レース番号を事前入力
14. 出走馬一括入力ボタン横に保存ボタンを複製配置
15. 「出走馬表登録」「払戻登録」ボタンの分割
16. 払戻入力欄のレイアウトをコンパクト化
17. カレンダー表示月のみレースを絞り込み表示

### 🅴 CSVインポート仕様変更
18. 外れ馬券も含め全件「確定」扱いに変更(payout=0)

### 🅵 集計画面
19. レース別収支を年月→日付→レースの3階層アコーディオン化

### 🅶 予想画面
20. 各馬のメモ有無をアコーディオンを開かず分かるようにする

### 🅷 モバイル対応 **実装済み(2026-08-02・フェーズ14)**
21. ~~小画面での組み合わせ選択表のレイアウト崩れ対応~~ 完了
22. ~~スマホで馬券履歴画面のCSVインポートUIを非表示に~~ 完了

### 🅸 保留・将来対応(現時点では着手しない)
23. 出走情報取得(外部データ連携・保留)
24. 払戻情報取得(外部データ連携・保留)
25. パスワードを忘れた場合の救済措置(将来)
26. 馬券履歴・集計ページの初期表示高速化(緊急性低・計測してから判断)

## 参考: 今回確認した仕様矛盾の解消方針

- 予想印「1頭1つまで」は反映済み(`docs/DESIGN.md`・`docs/TESTING.md`とも更新済み)
- 「レース確定後は購入不可」は一時実装したが2026-07-30に撤回し、確定後も購入可能な方針で反映済み(`docs/DESIGN.md`「ロック仕様」参照)
- 「確定後のチケット編集・削除禁止(ロック)」は2026-07-31に全面撤廃済み(`docs/DESIGN.md`「ロック仕様」参照)

## 出走情報・払戻情報の自動取得について(調査結果・2026-08-01)

「レース登録画面/払戻入力画面に取得ボタンを置き、開いているレース1件分だけをその場で取得する」という
UI案が出たが、1件ずつであっても呼び出せるHTTP経由の取得口がそもそも存在しないため、現状は保留とする。

- **JRA-VAN Data Lab.**(公式・月額2,090円): 唯一のインターフェースが「JV-Link」というWindows専用の
  ActiveX COMコンポーネント。Cloudflare Pages Functions(サーバーレスのJavaScript実行環境)からは
  技術的に呼び出せない。公式ドキュメント上もWindows Serverでの動作は非対応とされている
- **JRA公式サイト(jra.go.jp)への直接アクセス**: 実際にfetchで試したところ、bot対策により
  即座にブロックされることを確認した。利用規約の文言以前に、技術的にアクセスできない
- **netkeiba**: 利用規約でスクレイピングを明示的に禁止している(2024年以降Bot対策も強化)

選択肢(いずれも保留中。方針転換する場合は下記から選ぶ):

- A. 貼り付け方式を磨く方向で進める(現状維持・推奨)
- B. 有料のスクレイピング代行API(Bright Data等)を利用する。技術的には可能だが月額コストが発生し、
  対象サイトの規約違反リスクが完全になくなるわけではない
- C. Windows常時稼働機を別途用意し、JV-Linkを使った中継サーバーを立てる(JRA-VAN公式のため
  規約面は最もクリーンだが、インフラ運用の手間が大きい)

## 馬券かご 確定仕様(2026-08-01)

目的: 複数レース・複数式別にまたがる買い目を、都度保存せずに一時的にためておき、
最後にまとめて保存する。netkeiba・楽天競馬の「馬券かご」相当。

確定した仕様:

- 対象範囲は「レースをまたいでよい」。複数レース・複数式別の買い目を1つのかごに貯められる
- 現行の「式別を選んで組み合わせ選択→即保存」という都度保存の購入フローは廃止し、
  「かごに追加」→(必要なだけ繰り返す)→「まとめて購入する」の流れに一本化する
  (1点だけ買いたい場合も、必ずかごを経由する)
- かごの中身はブラウザ/端末を閉じたり、後日開き直しても保持されている必要がある。
  → サーバー保存(下書きテーブル)までは不要と判断し、localStorageに保存する方式とする
    (同一端末・同一ブラウザ内でのみ保持され、他の端末とは同期されない想定。複数端末での
    同期が必要になった場合は、サーバー側に下書き用テーブルを追加する改修が別途必要になる)
- 「まとめて購入する」実行時は、かご内の全買い目を1回のAPIリクエストで送信し、サーバー側は
  db.batch()でまとめて書き込む(フェーズ12の教訓を踏まえ、行ごとに個別リクエストを
  ループする実装にはしない)

画面イメージ:

- 購入画面(buy.html)に「かご」パネルを常設(右側 or 下部)。ヘッダー等にも
  「かご: n点 / 合計¥xxx」のように現在の内容を常時表示する
- かご内の各行: 日付・競馬場・レース番号・式別・買い目内容・金額・削除ボタン
- かご下部: 合計点数・合計金額、「まとめて購入する」ボタン、「かごを空にする」ボタン
- 保存成功後はかごを空にし(localStorageもクリア)、購入履歴に反映する

実装時の主な変更点(見積り):

- buy.js: 「購入する」ボタンを「かごに追加」ボタンに置き換え、かご状態(localStorage)の
  読み書き・かごパネルの描画・合計計算を追加。レース遷移時もかごの内容を保持する
- functions/api/tickets/bulk.js(または同等の一括保存API): 複数レース・複数式別が混在した
  買い目配列を受け取れるよう、リクエスト形式を拡張(現状は1レース分の想定になっていないか要確認)
- 将来の複数ユーザー対応(下記)と組み合わせる場合、localStorageのキーをログインユーザーごとに
  分ける(同じ端末を家族で共用した際にかごが混ざらないようにする)配慮が必要

## 複数ユーザー対応 確定仕様(2026-08-01)

目的: 家族それぞれが個別のログインID/パスワードを持ち、購入履歴・インポート履歴・
予想印/メモを完全に分離して管理できるようにする(近い将来の利用を見据えた対応。
まず2人分から開始し、将来的に人数が増える想定)。

確定した仕様:

- 認証方式: 家族それぞれに個別のログインID/パスワードを持たせる(現状の「全員共通の
  1つのパスワード」から廃止)。ログイン画面はユーザー名(テキスト入力)+パスワードの
  2項目に変更する
- データの可視性: 各ユーザーの購入履歴・インポート履歴・予想印・メモは完全に別々とし、
  他のユーザーからは見えないようにする(世帯合算ビューは不要)
- レース情報(races: 日付・競馬場・出走馬・着順・払戻など)は実世界の共通データであり、
  ユーザーごとに分ける対象ではない(全ユーザーで共有)。分離が必要なのは「誰が何を買ったか
  (tickets/imported_tickets/imported_ticket_groups/imported_ticket_items)」
  「誰の予想印・メモか(prediction_marks/prediction_notes/horse_notes)」の部分
- **レース情報の登録・編集(レース登録・出走馬表・着順・払戻の入力/編集、開催日程の
  一括登録)は管理者のみ**が行える。一般ユーザーはレース情報を閲覧・参照するのみ
- **CSVインポート時の自動レース登録は廃止する**。現状`functions/api/ticket-imports/index.js`
  のPOSTは、該当レースが`races`に無ければ自動的に新規作成しているが、レース登録が
  管理者限定になったことに伴い、この自動作成処理は削除する。該当レースが未登録のまま
  CSVを取り込んだ場合は`race_id`をnullのまま(日付・競馬場・レース番号・レース名は
  取込グループ側にそのまま保持)にしておき、後日管理者がそのレースを登録した際に
  紐付けられるようにする(既存のバックフィル的な仕組みを流用・拡張できないか、
  実装時に確認する)
- 「所有者を変更」する機能は**不要**(導入後は新規データが自動的にログインユーザーへ
  正しく紐づくため、後から変更する必要がない)。既存データ(個別ログイン導入前の
  購入履歴・インポート履歴)は、一括で代表ユーザー(管理者アカウント)に割り当てる
  だけでよい
- ユーザー登録: **ログイン画面に「新規ユーザー登録」を用意し、誰でもユーザー名(ID)と
  パスワードを自分で指定してその場でアカウント作成できる**(管理者による承認・招待コードは
  無し。アプリのURLを知っている前提でオープンに登録可能とする)
- 管理者判定: **Cloudflare Pagesの環境変数`ADMIN_USERNAMES`(カンマ区切り)に含まれる
  ユーザー名でログインした人だけが管理者として扱う**。DBに管理者フラグは持たせない
  (誤って書き換わるリスクがなく、環境変数を編集するだけで管理者を増減できるため)。
  最初の管理者は「先に通常のユーザー登録を行う→そのユーザー名を`ADMIN_USERNAMES`に
  追加する」という順番で設定する(鶏と卵の問題は発生しない)
- ユーザー削除機能: **今回は実装しない**(優先度低・将来必要になれば追加検討。実装コスト次第)
- パスワードを忘れた場合の救済措置(パスワードリセット等)は**今回は実装しない**。
  将来のタスクとして別途記録する(下記「未着手タスク一覧」参照)
- **管理者向け画面(2026-08-01追加・スコープに含める)**:
  1. **未登録レース一覧**: CSVインポートの自動レース登録を廃止したことに伴い、「CSVで取り込まれたが
     まだ`races`に登録されていない日付・競馬場・レース番号」の一覧を管理者向けに表示する。
     `imported_ticket_groups`のうち`race_id IS NULL`のものを日付・競馬場・レース番号でグルーピングして
     一覧化し、そこからレース登録画面へワンクリックで遷移できるようにする(遷移先で日付・競馬場・
     レース番号を事前入力しておくと親切)
  2. **登録ユーザー一覧**: `users`テーブルのユーザー名・登録日時を一覧表示する、管理者向けの
     閲覧専用画面(編集・削除機能は無し)。オープン登録(招待コード無し)のため、管理者が
     誰が登録したかを把握できるようにする目的

現状把握(2026-08-01時点のコード調査):

- 現在の認証はfunctions/_middleware.js + functions/api/auth/login.jsによる、
  環境変数APP_PASSWORDと照合するだけの全員共通パスワード方式。セッションCookieには
  有効期限(exp)のみが入っており、ユーザーを区別する情報は一切持っていない
- tickets・imported_tickets・imported_ticket_groups・imported_ticket_items・
  prediction_notes・prediction_marks・horse_notesのいずれのテーブルにも、
  所有者を表すカラムは存在しない

実装時の主な変更点(見積り・大規模になる想定):

1. usersテーブルを新設(id, username, password_hash, created_at)。管理者フラグはDBに
   持たせない(環境変数ADMIN_USERNAMESで判定するため)。パスワードはcrypto.subtleの
   PBKDF2等でハッシュ化して保存する
2. ログイン画面をパスワードのみ→ユーザー名+パスワードの入力に変更し、「新規ユーザー登録」
   導線を追加する。functions/api/auth/login.jsはusersテーブルと照合する方式に変更し、
   functions/api/auth/register.js(新規)でアカウント作成(作成後は自動ログイン)を行う
3. セッションCookieのペイロードにuser_id・usernameを含めるように変更し、
   functions/_middleware.jsで検証後、context.data.userId・context.data.isAdmin
   (ADMIN_USERNAMESとの突合結果)として後続のハンドラに渡す
4. tickets・imported_tickets・imported_ticket_groups・imported_ticket_items・
   prediction_notes・prediction_marks・horse_notesにuser_idカラムを追加する
   マイグレーション(schema.sql・latest*.sql・実データ移行スクリプトの3点が必要)。
   移行スクリプト自体では既存行のuser_idはNULLのままとし、管理者アカウント作成後に
   別途「既存データを管理者に割り当てる」SQLを1回実行する運用とする
5. 上記テーブルを読み書きするすべてのAPIエンドポイント(tickets、ticket-imports、
   prediction-notes、prediction-marks、horse-notes、stats集計まわり)に
   WHERE user_id = ?のフィルタ、および新規作成時のuser_idセットを追加する
   (touch pointが多いため、影響範囲の洗い出しに時間をかけて着手する)
6. races関連のAPI(レース登録・編集・出走馬表登録・払戻登録・開催日程一括登録・削除)に
   管理者権限チェック(context.data.isAdmin)を追加する
7. functions/api/ticket-imports/index.jsの「該当レースが無ければ自動作成する」処理を削除する
8. 管理者向け画面を新設: 「未登録レース一覧」(race_id未紐付けの取込グループをレース単位で
   集計して表示)、「登録ユーザー一覧」(users一覧の閲覧のみ)
9. 上記「馬券かご」機能と組み合わせる場合、localStorageのキーをユーザーごとに分ける
