# 未着手タスク・引継ぎメモ

このファイルは、要望としては承認済みだが未実装のタスクを引き継ぐための一覧です。
`README.md` / `docs/DESIGN.md` / `docs/TESTING.md` とは異なり「現状」ではなく
「これから対応すること」を記録する場所です。着手したら該当タスクをここから削除し、
`docs/DESIGN.md`(必要なら`docs/TESTING.md`も)に現状として反映してください。

※ JRAレース結果PDFインポート関連の詳細は docs/JRA_RESULT_PDF_IMPORT.md に統合しました（個別ログは archive/documents/ に移動済み）。

完了済みタスクの詳細な作業経緯(フェーズ1〜15)は`archive/documents/BACKLOG_HISTORY.md`
に退避してあります。新しいセッションで同じ修正を重複して行わないための参考としては
そちらを、次に何をやるかの判断には本ファイルを使ってください。

## 🔰 次��チャットで最初に読むこと(引き継ぎサマリー)

- アプリの全体像は`README.md`、データ構造・仕様の詳細は`docs/DESIGN.md`、
  手動テスト項目は`docs/TESTING.md`を参照(いずれも現状を反映した生きたドキュメント)。
  過去の作業経緯は`archive/documents/BACKLOG_HISTORY.md`参照
- 作業の進め方は毎回同じパターン: **①要望の仕様矛盾・不足を確認→承認を得る
  →②`docs/DESIGN.md`等を仕様に合わせて更新→③実装→④`node --check`で構文チェック
  →⑤`docs/BACKLOG.md`のタスク表・完了履歴を更新→⑥zipに固めてダウンロード提供**
- 実機(wrangler dev / 本番)での動作確認は毎回**未実施**。コードレビューと
  Node上でのロジック単体テスト・シミュレーションのみで検証している。
  実機確認はユーザー側で行ってもらっている状況
- **2026-08-10: クラスタ5(prediction.js)の「各馬にメモがあるかどうかを、
  アコーディオンを開かなくても分かるようにする」を実装済み**。`public/prediction.js`の
  `renderHorses()`で、馬メモが登録済みの馬の馬名右に「メモ」バッジ(`.horse-note-badge`,
  hankoの赤アクセントの縁取り)を追加した。以前は薄いグレーの小さいプレビュー文字
  (`.horse-note-preview`)だけが頼りで、一覧を流し見すると見落としやすかった問題への
  対応。プレビュー文字自体は引き続き表示し、内容の一部を確認できるようにしている
  (バッジ側の「メモ」表記と重複しないよう、プレビュー文字側の「メモ: 」プレフィックスは
  削除した)。`public/style.css`に`.horse-note-badge`のスタイルを追加(新規クラスの
  追加のみで既存クラスの変更は無し)。`docs/DESIGN.md`の予想登録画面レイアウトの節、
  `docs/TESTING.md`「2.0.1」にそれぞれ反映済み

...（以降は既存の BACKLOG.md の残りをそのまま）
