# 未着手タスク・引継ぎメモ

このファイルは、要望としては承認済みだが未実装のタスクを引き継ぐための一覧です。
`README.md` / `docs/DESIGN.md` / `docs/TESTING.md` とは異なり「現状」ではなく
「これから対応すること」を記録する場所です。着手したら該当タスクをここから削除し、
`docs/DESIGN.md`(必要なら`docs/TESTING.md`も)に現状として反映してください。

## 2026-07-30 時点の状況

直近の要望のうち、以下2件のバグ修正のみ着手・完了しました(詳細は`docs/DESIGN.md`参照)。

- CSV取込: 複数的中組み合わせの払戻金額計算、重複判定キーへの日付追加
- レース新規登録時に着順・払戻が保存されない不具合の修正

残りの要望は未着手です。優先度・規模の見積もりとあわせて以下に記録します。

## 承認済みの仕様決定(未実装)

実装時は以下の方針で進めること。

- **予想印は1頭につき1つまで**(現状は複数登録可能な仕様。ドロップダウン化とあわせて
  「1頭1印」に変更してよいと承認済み。実装時は`docs/TESTING.md`の
  「1頭に複数の印を登録できること」の項目を削除・修正すること)
- **レース確定後(着順 or 払戻のいずれかが入力済み)は、そのレースの新規馬券購入も禁止する**
  (現状のロック仕様は購入済みチケットの変更・削除禁止のみ。新規購入自体の禁止を追加する。
  実装時は`docs/DESIGN.md`「ロック仕様」の節を更新すること)

## 未着手タスク一覧(優先度・規模つき)

| 優先度 | 内容 | 規模目安 | 備考 |
|---|---|---|---|
| 高 | 騎手名を馬名の後ろに表示(現状は下、18頭立てでスクロールが必要) | 小 | `prediction.js`のカード表示のみ |
| 高 | 出走頭数を5〜18頭で可変に(現状8固定) | 小〜中 | `races.js`の出走馬行生成ロジック |
| 高 | レース編集画面: 馬名・着順なしでも常に払戻金額を入力できるようにする。1〜3着の馬番はセレクトなどで選べるようにし、UIをコンパクトに | 中 | `races.js`の払戻セクションの作り直し。現状は`finish_order`が無いと払戻入力欄自体が出ない |
| 高 | レース確定後(着順 or 払戻確定)はそのレースの馬券購入を禁止する | 中 | 上記「承認済みの仕様決定」参照。`buy.js`+APIの両方で制御が必要 |
| 高 | 払戻入力画面の「0点購入」表示を非表示にする(管理画面なので不要) | 小 | `races.js`の`${betTickets.length}点購入`表示 |
| 中 | 予想印をドロップダウン化(1頭1印) | 小 | 上記「承認済みの仕様決定」参照 |
| 中 | レース管理画面のヘッダーナビを「馬券購入」のみに(一般ユーザー画面への遷移を削除) | 小 | `races.html`のnav |
| 中 | 購入履歴(index.html)画面: 最初からカレンダー表示、「日付から選ぶ」ボタンを削除。初期表示は総購入・総払戻・収支・回収率・カレンダーのみとし、詳細はカレンダーで選択した日付分だけ表示 | 小〜中 | カレンダー自体は実装済み。初期状態と表示ロジックの変更が中心 |
| 中 | 出走頭数選択時に枠番の初期値を自動設定する | 小 | `races.js`の出走馬行生成 |
| 中 | レース管理画面にカレンダーから日付を選べるようにする | 中 | 既存のカレンダー部品(index.html/app.js)を流用 |
| 中 | CSV取込結果のうち着順・払戻が判明した情報を、レースの着順・払戻の項目へ反映する | 中 | 現状は3連単の的中買い目が一意に取れる場合のみ`finish_order`を補足。他式別・払戻レートへの反映は未実装 |
| 中 | 集計画面(レース別収支・競馬場別・騎手別収支)の列ソート機能(日付・競馬場・レース番号順が初期、各列クリックで昇順降順切替) | 中 | 同じ実装パターンを3画面に展開 |
| 中 | 三連単・三連複の2頭軸購入UIをJRA/netkeiba風に | 中〜大 | 具体的な挙動(軸の選び方・相手の選び方のUI)は次回要ヒアリング |
| 中 | 予想登録画面のレイアウト再設計一式: 購入馬券履歴を下部に・馬一覧を上部に/「このレースの馬券を購入」ボタンをレース名の横(上部)に配置/競馬場名をセレクト風にして切替可能に/レース番号もセレクト風にして表示レースを切替可能に(カレンダー画面へ戻る導線がない問題への対応)/日付を競馬場名の左に表示 | 大 | 1画面のレイアウト再設計としてまとめて着手するのが効率的 |
| 低(要調査) | 馬券履歴ページ・集計ページの初期表示の高速化 | 不明 | まず現状のボトルネックを調査してから規模を見積もる |

## 参考: 今回確認した仕様矛盾の解消方針(反映済み)

- 予想印「1頭1つまで」は`docs/TESTING.md`の既存記載と矛盾するため、実装時に同ファイルを更新すること
- 「レース確定後は購入不可」は`docs/DESIGN.md`のロック仕様(購入済みチケットの変更・削除禁止)とは別の、
  新規追加のルールであることに注意
